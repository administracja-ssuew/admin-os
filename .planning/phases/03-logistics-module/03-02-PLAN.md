---
phase: 03-logistics-module
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - types/index.ts
  - hooks/useLogisticsData.ts
autonomous: true
requirements: [LOG-01, LOG-03, LOG-04]

must_haves:
  truths:
    - "Interfejs EquipmentLoan zawiera borrower_phone, borrower_org, loan_source (bez deposit_*)"
    - "Interfejs Asset zawiera quantity, min_quantity, unit"
    - "useLogisticsData zwraca lowStockAssets jako przefiltrowaną listę (quantity < min_quantity)"
    - "Hook kompiluje się bez błędów TypeScript"
  artifacts:
    - path: "types/index.ts"
      provides: "Zaktualizowane interfejsy EquipmentLoan i Asset"
      contains: "borrower_phone"
    - path: "hooks/useLogisticsData.ts"
      provides: "Logika low stock obliczana po stronie aplikacji"
      exports: ["UseLogisticsDataResult", "useLogisticsData"]
  key_links:
    - from: "hooks/useLogisticsData.ts"
      to: "types/index.ts"
      via: "import type { Asset, EquipmentLoan }"
      pattern: "import type.*EquipmentLoan"
    - from: "hooks/useLogisticsData.ts"
      to: "supabase assets table"
      via: "select z nowymi kolumnami"
      pattern: "quantity.*min_quantity.*unit"
---

<objective>
Aktualizacja interfejsów TypeScript i hooka danych dla modułu Logistyki — dodanie nowych pól i logiki low stock.

Purpose: Warstwa typów i danych musi odzwierciedlać schemat DB przed aktualizacją UI. Logika low_stock obliczana w hooku (per D-05), bez DB trigger.
Output: Zaktualizowane types/index.ts i hooks/useLogisticsData.ts z pełnym typowaniem i logiką low stock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-logistics-module/03-CONTEXT.md
@.planning/phases/03-logistics-module/03-01-SUMMARY.md

@types/index.ts
@hooks/useLogisticsData.ts
</context>

<interfaces>
<!-- Aktualne interfejsy do modyfikacji. Przeczytaj poniższe PRZED wprowadzeniem zmian. -->

Z types/index.ts (stan przed modyfikacją):
```typescript
export interface Asset {
  id: string
  name: string
  asset_type: AssetType
  status: AssetStatus
  location: string | null
  notes: string | null
  created_at: string
  // BRAK: quantity, min_quantity, unit
}

export interface EquipmentLoan {
  id: string
  agreement_number: string | null
  item_category: LoanItemCategory
  borrower_name: string
  issue_date: string
  return_date: string | null
  status: LoanStatus
  notes: string | null
  created_at: string
  // BRAK: borrower_phone, borrower_org, loan_source
}
```

Z hooks/useLogisticsData.ts (stan przed modyfikacją):
```typescript
export interface UseLogisticsDataResult {
  assets: Asset[]
  loans: EquipmentLoan[]
  reports: LogisticsReport[]
  members: DeptMember[]
  loading: boolean
  refetch: () => Promise<void>
  // BRAK: lowStockAssets
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Zaktualizuj interfejsy TypeScript w types/index.ts</name>
  <files>types/index.ts</files>
  <action>
    Zaktualizuj dwa interfejsy w sekcji "TYPY MY-DEPARTMENT (Phase 2 Refactor)".

    KRYTYCZNE (per D-01): NIE dodawaj deposit_required ani deposit_amount do EquipmentLoan.

    1. Rozszerz interfejs Asset — dodaj po polu `notes`:
    ```typescript
    quantity: number
    min_quantity: number
    unit: 'szt' | 'ryza' | 'opak' | 'komplet'
    ```

    2. Rozszerz interfejs EquipmentLoan — dodaj po polu `borrower_name`:
    ```typescript
    borrower_phone: string | null
    borrower_org: string | null
    loan_source: string | null
    ```

    Nie zmieniaj żadnych innych interfejsów ani typów. Nie usuwaj istniejących pól.
    Zachowaj komentarz sekcji `// ─── TYPY MY-DEPARTMENT (Phase 2 Refactor) ────`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>types/index.ts kompiluje się bez błędów. Asset ma quantity/min_quantity/unit. EquipmentLoan ma borrower_phone/borrower_org/loan_source. Brak deposit_*.</done>
</task>

<task type="auto">
  <name>Task 2: Zaktualizuj useLogisticsData o nowe pola i lowStockAssets</name>
  <files>hooks/useLogisticsData.ts</files>
  <action>
    Zaktualizuj hook w trzech miejscach:

    1. Zapytanie Supabase dla assets — zmień select na jawne kolumny zamiast '*', aby TypeScript widział nowe pola:
    ```typescript
    supabase.from('assets').select('id, name, asset_type, status, location, notes, created_at, quantity, min_quantity, unit').order('name', { ascending: true })
    ```

    2. Rozszerz UseLogisticsDataResult o pole lowStockAssets:
    ```typescript
    export interface UseLogisticsDataResult {
      assets: Asset[]
      loans: EquipmentLoan[]
      reports: LogisticsReport[]
      members: DeptMember[]
      lowStockAssets: Asset[]   // per D-05: obliczane w hooku
      loading: boolean
      refetch: () => Promise<void>
    }
    ```

    3. W ciele hooka oblicz lowStockAssets jako pochodną stanu assets (PO setAssets):
    ```typescript
    // Logika low stock po stronie aplikacji (per D-05) — bez DB trigger
    const lowStockAssets = assets.filter(a => a.quantity < a.min_quantity && a.min_quantity > 0)
    ```

    4. Zwróć lowStockAssets w returnie hooka:
    ```typescript
    return { assets, loans, reports, members, lowStockAssets, loading, refetch: fetchData }
    ```

    UWAGA: lowStockAssets to pochodna wartość obliczana przy każdym renderze z aktualnego stanu assets — nie jest osobnym wywołaniem Supabase. Warunek `min_quantity > 0` zabezpiecza przed fałszywymi alarmami gdy min_quantity nie jest jeszcze ustawione.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Hook kompiluje się bez błędów TS. UseLogisticsDataResult eksportuje lowStockAssets: Asset[]. Logika: quantity &lt; min_quantity &amp;&amp; min_quantity &gt; 0.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero błędów
- `grep -n "lowStockAssets" hooks/useLogisticsData.ts` — 3 wystąpienia (definicja w interfejsie, obliczenie, return)
- `grep "deposit" types/index.ts` — brak wyników (lub tylko w innych interfejsach niezwiązanych z EquipmentLoan)
- `grep -A5 "interface EquipmentLoan" types/index.ts` — zawiera borrower_phone, borrower_org, loan_source
</verification>

<success_criteria>
Projekt kompiluje się bez błędów TypeScript. EquipmentLoan ma 3 nowe pola kontaktowe (bez deposit_*). Asset ma quantity/min_quantity/unit. useLogisticsData eksportuje lowStockAssets obliczone po stronie aplikacji.
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/03-logistics-module/03-02-SUMMARY.md` z listą zmian w interfejsach i hooku.
</output>
