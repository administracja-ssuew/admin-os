---
phase: 03-logistics-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260404_logistics_schema_extension.sql
autonomous: true
requirements: [LOG-01, LOG-03]

must_haves:
  truths:
    - "Tabela equipment_loans posiada kolumny borrower_phone, borrower_org, loan_source"
    - "Tabela assets posiada kolumny quantity, min_quantity, unit"
    - "Migracja aplikuje się bez błędów na bazie Supabase"
  artifacts:
    - path: "supabase/migrations/20260404_logistics_schema_extension.sql"
      provides: "Rozszerzenie schematu equipment_loans i assets"
      contains: "ALTER TABLE equipment_loans"
  key_links:
    - from: "supabase/migrations/20260404_logistics_schema_extension.sql"
      to: "equipment_loans"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "borrower_phone|borrower_org|loan_source"
    - from: "supabase/migrations/20260404_logistics_schema_extension.sql"
      to: "assets"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "min_quantity|unit"
---

<objective>
Rozszerzenie schematu bazy danych dla modułu Logistyki: nowe kolumny w equipment_loans (dane pożyczkobiorcy) i assets (śledzenie stanu magazynowego).

Purpose: Zapewnienie kolumn DB wymaganych przez LOG-01 i LOG-03 przed aktualizacją hooka i UI.
Output: Plik migracji SQL gotowy do wykonania przez Supabase CLI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-logistics-module/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migracja SQL — rozszerzenie equipment_loans i assets</name>
  <files>supabase/migrations/20260404_logistics_schema_extension.sql</files>
  <action>
    Utwórz plik migracji SQL rozszerzający dwie tabele.

    KRYTYCZNE (per D-01): BEZ pól kaucji — NIE dodawaj deposit_required ani deposit_amount.

    Dla tabeli equipment_loans dodaj kolumny:
    - borrower_phone TEXT — telefon pożyczkobiorcy (nullable)
    - borrower_org TEXT — organizacja pożyczkobiorcy (nullable)
    - loan_source TEXT — źródło wypożyczenia (nullable), np. 'wewnętrzne', 'zewnętrzne'

    Dla tabeli assets dodaj kolumny:
    - quantity INTEGER DEFAULT 0 — bieżący stan magazynowy
    - min_quantity INTEGER DEFAULT 0 — próg niskiego stanu
    - unit TEXT DEFAULT 'szt' — jednostka; CHECK constraint: unit IN ('szt', 'ryza', 'opak', 'komplet') (per D-04)

    Użyj ADD COLUMN IF NOT EXISTS, aby migracja była idempotentna.

    Struktura pliku:
    ```sql
    -- Logistics Module: schema extension (Phase 3, Plan 01)
    -- LOG-01: borrower contact fields on equipment_loans (BEZ deposit_*)
    -- LOG-03: stock tracking fields on assets

    ALTER TABLE equipment_loans
      ADD COLUMN IF NOT EXISTS borrower_phone TEXT,
      ADD COLUMN IF NOT EXISTS borrower_org TEXT,
      ADD COLUMN IF NOT EXISTS loan_source TEXT;

    ALTER TABLE assets
      ADD COLUMN IF NOT EXISTS quantity INTEGER DEFAULT 0,
      ADD COLUMN IF NOT EXISTS min_quantity INTEGER DEFAULT 0,
      ADD COLUMN IF NOT EXISTS unit TEXT DEFAULT 'szt'
        CONSTRAINT assets_unit_check CHECK (unit IN ('szt', 'ryza', 'opak', 'komplet'));
    ```

    UWAGA: Nie dodawaj polityk RLS — tabela assets i equipment_loans mają już istniejące polityki z Phase 2. Nowe kolumny dziedziczą istniejące RLS.
  </action>
  <verify>
    <automated>grep -q "borrower_phone" supabase/migrations/20260404_logistics_schema_extension.sql && grep -q "min_quantity" supabase/migrations/20260404_logistics_schema_extension.sql && grep -q "deposit" supabase/migrations/20260404_logistics_schema_extension.sql | grep -v "deposit" && echo "OK" || echo "FAIL: brakuje kolumn lub wykryto deposit_*"</automated>
  </verify>
  <done>Plik migracji istnieje z kolumnami borrower_phone, borrower_org, loan_source w equipment_loans oraz quantity, min_quantity, unit z CHECK constraint w assets. Brak deposit_required i deposit_amount.</done>
</task>

<task type="auto">
  <name>Task 2: Zastosuj migrację przez Supabase CLI</name>
  <files></files>
  <action>
    Wykonaj migrację na połączonej bazie Supabase.

    Sprawdź najpierw dostępność CLI:
    ```bash
    npx supabase db push
    ```

    Jeśli Supabase CLI nie jest podłączone do projektu (brak supabase/config.toml lub brak połączenia), wykonaj SQL bezpośrednio przez panel Supabase SQL Editor — wyświetl zawartość pliku migracji i poinstruuj użytkownika, że musi ją skopiować ręcznie (checkpoint:human-action dla tego przypadku).

    Po pomyślnym push zweryfikuj kolumny:
    ```bash
    npx supabase db execute --sql "SELECT column_name FROM information_schema.columns WHERE table_name = 'equipment_loans' AND column_name IN ('borrower_phone', 'borrower_org', 'loan_source');"
    ```
  </action>
  <verify>
    <automated>npx supabase db execute --sql "SELECT COUNT(*) FROM information_schema.columns WHERE table_name IN ('equipment_loans', 'assets') AND column_name IN ('borrower_phone', 'borrower_org', 'loan_source', 'quantity', 'min_quantity', 'unit');" 2>/dev/null || echo "Sprawdź ręcznie w Supabase Dashboard — wszystkie 6 kolumn musi istnieć"</automated>
  </verify>
  <done>Wszystkie 6 nowych kolumn istnieje w bazie: borrower_phone, borrower_org, loan_source w equipment_loans oraz quantity, min_quantity, unit w assets.</done>
</task>

</tasks>

<verification>
Sprawdź plik migracji:
- `grep -c "ADD COLUMN" supabase/migrations/20260404_logistics_schema_extension.sql` — powinno zwrócić 6
- `grep "deposit" supabase/migrations/20260404_logistics_schema_extension.sql` — powinno zwrócić pusty wynik (brak pól kaucji)
- `grep "unit IN" supabase/migrations/20260404_logistics_schema_extension.sql` — powinno zawierać CHECK constraint z opcjami szt/ryza/opak/komplet
</verification>

<success_criteria>
Migracja SQL istnieje i jest poprawna: 3 kolumny w equipment_loans (bez deposit_*), 3 kolumny w assets z CHECK constraint na unit. Baza zaktualizowana lub plik gotowy do ręcznego wykonania.
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/03-logistics-module/03-01-SUMMARY.md` z listą dodanych kolumn i statusem migracji DB.
</output>
