---
phase: 03-logistics-module
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - components/subcommittees/LogisticsPanel.tsx
  - components/subcommittees/AssetInventory.tsx
autonomous: false
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, LOG-05]

must_haves:
  truths:
    - "Formularz nowej umowy zawiera pola borrower_phone, borrower_org, loan_source (bez pól kaucji)"
    - "Wiersze przeterminowanych wypożyczeń (status Wypożyczone + return_date w przeszłości) mają czerwony badge"
    - "Komponent AssetInventory wyświetla quantity, unit i badge Niski stan gdy quantity < min_quantity"
    - "Zmiana statusu wypożyczenia wywołuje logAudit() z oldValue i newValue"
  artifacts:
    - path: "components/subcommittees/LogisticsPanel.tsx"
      provides: "Rozszerzony formularz umów + overdue detection + audit"
      contains: "borrower_phone"
    - path: "components/subcommittees/AssetInventory.tsx"
      provides: "Komponent inwentarza materiałów biurowych z low stock badge"
      exports: ["AssetInventory"]
  key_links:
    - from: "components/subcommittees/LogisticsPanel.tsx"
      to: "lib/audit.ts"
      via: "logAudit() w toggleLoanStatus"
      pattern: "logAudit.*equipment_loan"
    - from: "components/subcommittees/AssetInventory.tsx"
      to: "types/index.ts"
      via: "import type { Asset }"
      pattern: "import type.*Asset"
    - from: "components/subcommittees/LogisticsPanel.tsx"
      to: "components/subcommittees/AssetInventory.tsx"
      via: "import i montaż komponentu"
      pattern: "<AssetInventory"
---

<objective>
Rozszerzenie UI modułu Logistyki: formularz umów z nowymi polami, czerwone badge dla przeterminowanych, komponent AssetInventory z low stock, audit przy zmianie statusu.

Purpose: Wszystkie 5 wymagań LOG-01..LOG-05 zamknięte w jednym planie UI — dane i typy są już gotowe po planach 01 i 02.
Output: Kompletny moduł Logistyki widoczny i działający w LogisticsPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-logistics-module/03-CONTEXT.md
@.planning/phases/03-logistics-module/03-02-SUMMARY.md

@components/subcommittees/LogisticsPanel.tsx
@hooks/useLogisticsData.ts
@types/index.ts
@lib/audit.ts
</context>

<interfaces>
<!-- Kontrakty do użycia w implementacji. -->

Z types/index.ts (po planie 02):
```typescript
export interface EquipmentLoan {
  id: string
  agreement_number: string | null
  item_category: LoanItemCategory
  borrower_name: string
  borrower_phone: string | null   // NOWE
  borrower_org: string | null     // NOWE
  loan_source: string | null      // NOWE
  issue_date: string
  return_date: string | null
  status: LoanStatus
  notes: string | null
  created_at: string
}

export interface Asset {
  id: string
  name: string
  asset_type: AssetType
  status: AssetStatus
  location: string | null
  notes: string | null
  quantity: number          // NOWE
  min_quantity: number      // NOWE
  unit: 'szt' | 'ryza' | 'opak' | 'komplet'  // NOWE
  created_at: string
}
```

Z hooks/useLogisticsData.ts (po planie 02):
```typescript
export interface UseLogisticsDataResult {
  assets: Asset[]
  loans: EquipmentLoan[]
  reports: LogisticsReport[]
  members: DeptMember[]
  lowStockAssets: Asset[]   // obliczane w hooku
  loading: boolean
  refetch: () => Promise<void>
}
```

Z lib/audit.ts:
```typescript
export async function logAudit({
  userId, action, entityType, entityId, oldValue?, newValue?
}: AuditLogParams): Promise<void>
```

Istniejący stan loanForm w LogisticsPanel (do rozszerzenia):
```typescript
const [loanForm, setLoanForm] = useState({
  agreement_number: '',
  item_category: 'Namiot Plenerowy',
  borrower_name: '',
  issue_date: '',
  return_date: '',
  status: 'Wypożyczone',
  notes: '',
  // dodaj: borrower_phone, borrower_org, loan_source
})
```

Istniejąca funkcja toggleLoanStatus (do rozszerzenia o audit):
```typescript
const toggleLoanStatus = async (id: string, currentStatus: string) => {
  const newStatus = currentStatus === 'Wypożyczone' ? 'Zwrócone' : 'Wypożyczone'
  await supabase.from('equipment_loans').update({ status: newStatus, ... }).eq('id', id)
  await onRefetch()
  // BRAK logAudit — dodaj
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rozszerz formularz umów i dodaj overdue detection + audit w LogisticsPanel</name>
  <files>components/subcommittees/LogisticsPanel.tsx</files>
  <action>
    Wprowadź trzy zmiany w LogisticsPanel.tsx:

    **A. Rozszerzenie loanForm o nowe pola (per D-01, LOG-01):**
    Zmień inicjalny stan loanForm dodając:
    ```typescript
    borrower_phone: '',
    borrower_org: '',
    loan_source: '',
    ```
    Oraz w resetowaniu formularza po sukcesie (handleAddLoan).

    W JSX formularza modalnego dodaj trzy nowe pola INPUT po polu borrower_name:
    ```tsx
    <input
      type="tel"
      placeholder="Telefon pożyczkobiorcy"
      value={loanForm.borrower_phone}
      onChange={e => setLoanForm(f => ({ ...f, borrower_phone: e.target.value }))}
      className="w-full px-3 py-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-sm"
    />
    <input
      type="text"
      placeholder="Organizacja"
      value={loanForm.borrower_org}
      onChange={e => setLoanForm(f => ({ ...f, borrower_org: e.target.value }))}
      className="w-full px-3 py-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-sm"
    />
    <select
      value={loanForm.loan_source}
      onChange={e => setLoanForm(f => ({ ...f, loan_source: e.target.value }))}
      className="w-full px-3 py-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-sm"
    >
      <option value="">Źródło wypożyczenia</option>
      <option value="wewnętrzne">Wewnętrzne</option>
      <option value="zewnętrzne">Zewnętrzne</option>
    </select>
    ```

    **B. Overdue detection — czerwony badge (per D-02, LOG-02):**
    Dodaj pochodną wartość po bloku filteredLoans:
    ```typescript
    const today = new Date().toISOString().split('T')[0]
    const isOverdue = (loan: EquipmentLoan) =>
      loan.status === 'Wypożyczone' && loan.return_date !== null && loan.return_date < today
    ```

    W tabeli wypożyczeń, przy renderowaniu wiersza, dodaj badge obok nazwy pożyczkobiorcy:
    ```tsx
    {isOverdue(loan) && (
      <span className="ml-2 px-2 py-0.5 rounded-full text-xs font-bold bg-red-100 text-red-700 dark:bg-red-900/40 dark:text-red-400">
        Przeterminowane
      </span>
    )}
    ```
    Opcjonalnie tło wiersza: `className={isOverdue(loan) ? 'bg-red-50 dark:bg-red-900/10' : ''}` na `<tr>`.

    **C. Audit przy zmianie statusu (per D-07, LOG-05):**
    Zaktualizuj toggleLoanStatus — dodaj logAudit po pomyślnym update:
    ```typescript
    const toggleLoanStatus = async (id: string, currentStatus: string) => {
      const newStatus = currentStatus === 'Wypożyczone' ? 'Zwrócone' : 'Wypożyczone'
      const toastId = toast.loading('Aktualizowanie statusu...')
      const { error } = await supabase.from('equipment_loans').update({
        status: newStatus,
        return_date: newStatus === 'Zwrócone' ? new Date().toISOString().split('T')[0] : null,
      }).eq('id', id)
      if (!error) {
        if (currentUser) {
          await logAudit({
            userId: currentUser.id,
            action: 'UPDATE_STATUS',
            entityType: 'equipment_loan',
            entityId: id,
            oldValue: { status: currentStatus },
            newValue: { status: newStatus },
          })
        }
        toast.success('Status zaktualizowany', { id: toastId })
        await onRefetch()
      } else {
        toast.error('Błąd aktualizacji statusu', { id: toastId })
      }
    }
    ```

    Upewnij się, że logAudit jest już importowane (jest — linia 6 w istniejącym pliku).
    Upewnij się, że komponent przyjmuje lowStockAssets z propsów jeśli potrzebne, lub importuje AssetInventory i przekazuje assets + lowStockAssets.

    Na dole komponentu, w sekcji JSX, dodaj sekcję AssetInventory po istniejącej sekcji assets:
    ```tsx
    {/* Inwentarz materiałów biurowych */}
    <AssetInventory assets={assets} lowStockAssets={lowStockAssets} />
    ```
    gdzie lowStockAssets pochodzi z props (rozszerz LogisticsPanelProps o `lowStockAssets: Asset[]`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Formularz zawiera borrower_phone, borrower_org, loan_source. Przeterminowane wiersze mają czerwony badge. toggleLoanStatus wywołuje logAudit z oldValue/newValue. Komponent montuje AssetInventory.</done>
</task>

<task type="auto">
  <name>Task 2: Utwórz komponent AssetInventory</name>
  <files>components/subcommittees/AssetInventory.tsx</files>
  <action>
    Utwórz nowy komponent AssetInventory jako osobny plik.

    Konwencje (z CONVENTIONS.md):
    - Pierwsza linia: `'use client'`
    - Importy w kolejności: React hooks, supabase, internal hooks, components, lib, external, types
    - Względne ścieżki: `../../types`, `../../lib/supabase`, itp.
    - Styl Tailwind: dark: warianty wszędzie
    - Brak średników na końcu linii
    - Cudzysłowy pojedyncze

    Interfejs propsów:
    ```typescript
    interface AssetInventoryProps {
      assets: Asset[]
      lowStockAssets: Asset[]
    }
    ```

    Komponent renderuje:
    1. Nagłówek sekcji z tytułem "Materiały biurowe" i licznikiem Niski stan (lowStockAssets.length).
    2. Tabelę z kolumnami: Nazwa, Ilość, Jednostka, Status (badge).
    3. Dla każdego asset gdzie asset_type === 'Artykuły biurowe' lub quantity/min_quantity są ustawione:
       - Wyświetl quantity i unit
       - Badge "Niski stan" gdy quantity < min_quantity && min_quantity > 0 (per D-05):
         ```tsx
         {asset.min_quantity > 0 && asset.quantity < asset.min_quantity && (
           <span className="px-2 py-0.5 rounded-full text-xs font-bold bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-400">
             Niski stan
           </span>
         )}
         ```
    4. Stan pusty gdy brak assets z polami quantity: wyświetl `<EmptyState message="Brak materiałów biurowych" />` lub prostą wiadomość.

    UWAGA: AssetInventory jest komponentem read-only — nie zawiera mutacji Supabase. Mutacje pozostają w LogisticsPanel. Komponent tylko wyświetla dane.

    Pełna struktura pliku:
    ```typescript
    'use client'

    import type { Asset } from '../../types'

    interface AssetInventoryProps {
      assets: Asset[]
      lowStockAssets: Asset[]
    }

    export default function AssetInventory({ assets, lowStockAssets }: AssetInventoryProps) {
      const officeAssets = assets.filter(a => a.asset_type === 'Artykuły biurowe' || a.min_quantity > 0)

      return (
        <div className="bg-white dark:bg-slate-800 rounded-2xl border border-slate-200 dark:border-slate-700 p-5">
          <div className="flex items-center justify-between mb-4">
            <h3 className="text-base font-bold text-slate-800 dark:text-white">
              Materiały biurowe
            </h3>
            {lowStockAssets.length > 0 && (
              <span className="px-2 py-1 rounded-full text-xs font-bold bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-400">
                {lowStockAssets.length} niski stan
              </span>
            )}
          </div>
          {officeAssets.length === 0 ? (
            <p className="text-sm text-slate-400 dark:text-slate-500 text-center py-8">
              Brak materiałów biurowych do wyświetlenia
            </p>
          ) : (
            <div className="overflow-x-auto">
              <table className="w-full text-sm">
                <thead>
                  <tr className="border-b border-slate-200 dark:border-slate-700">
                    <th className="text-left py-2 px-3 font-semibold text-slate-600 dark:text-slate-400">Nazwa</th>
                    <th className="text-right py-2 px-3 font-semibold text-slate-600 dark:text-slate-400">Ilość</th>
                    <th className="text-left py-2 px-3 font-semibold text-slate-600 dark:text-slate-400">Jed.</th>
                    <th className="text-left py-2 px-3 font-semibold text-slate-600 dark:text-slate-400">Status</th>
                  </tr>
                </thead>
                <tbody>
                  {officeAssets.map(asset => (
                    <tr key={asset.id} className="border-b border-slate-100 dark:border-slate-700/50 last:border-0">
                      <td className="py-2 px-3 text-slate-800 dark:text-white">{asset.name}</td>
                      <td className="py-2 px-3 text-right font-mono text-slate-700 dark:text-slate-300">{asset.quantity}</td>
                      <td className="py-2 px-3 text-slate-500 dark:text-slate-400">{asset.unit}</td>
                      <td className="py-2 px-3">
                        {asset.min_quantity > 0 && asset.quantity < asset.min_quantity ? (
                          <span className="px-2 py-0.5 rounded-full text-xs font-bold bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-400">
                            Niski stan
                          </span>
                        ) : (
                          <span className="px-2 py-0.5 rounded-full text-xs font-bold bg-green-100 text-green-700 dark:bg-green-900/40 dark:text-green-400">
                            OK
                          </span>
                        )}
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AssetInventory.tsx istnieje jako default export. Wyświetla tabelę z quantity, unit, badge "Niski stan" gdy quantity &lt; min_quantity. Komponent kompiluje się bez błędów TS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Kompletny moduł Logistyki:
    - Formularz umów z polami borrower_phone, borrower_org, loan_source (bez kaucji)
    - Czerwony badge "Przeterminowane" dla wypożyczeń po terminie
    - Komponent AssetInventory z badge "Niski stan"
    - Audit log przy zmianie statusu wypożyczenia
  </what-built>
  <how-to-verify>
    1. Uruchom aplikację: `npm run dev` → otwórz http://localhost:3000
    2. Przejdź do sekcji "Mój Departament" → wybierz departament Logistyki
    3. Kliknij "Dodaj umowę" — sprawdź czy formularz zawiera pola: Telefon, Organizacja, Źródło (BEZ pól kaucji)
    4. Dodaj testową umowę z datą zwrotu w przeszłości i statusem "Wypożyczone" — sprawdź czy pojawia się czerwony badge "Przeterminowane"
    5. Kliknij przycisk zmiany statusu na istniejącym wypożyczeniu — sprawdź w Supabase Dashboard → tabela audit_log czy pojawił się nowy wpis z entity_type='equipment_loan'
    6. Sprawdź sekcję "Materiały biurowe" — czy tabela AssetInventory jest widoczna z kolumnami Ilość i Jednostka
    7. Jeśli masz zasób z quantity &lt; min_quantity — sprawdź czy badge "Niski stan" jest widoczny
  </how-to-verify>
  <resume-signal>Wpisz "zatwierdzone" lub opisz napotkane problemy</resume-signal>
</task>

</tasks>

<verification>
Po checkpoint:
- `npx tsc --noEmit` — zero błędów
- `npm run lint` — zero nowych ostrzeżeń ESLint
- `grep -n "deposit" components/subcommittees/LogisticsPanel.tsx` — brak wyników
- `grep -n "logAudit" components/subcommittees/LogisticsPanel.tsx` — co najmniej 2 wystąpienia (delete + toggle)
- `grep -n "Przeterminowane" components/subcommittees/LogisticsPanel.tsx` — co najmniej 1 wystąpienie
</verification>

<success_criteria>
Formularz umów zawiera borrower_phone, borrower_org, loan_source — bez pól kaucji (per D-01). Przeterminowane wiersze mają czerwony badge (per D-02). AssetInventory wyświetla inwentarz z badge "Niski stan" (per D-05). logAudit() wywoływane przy toggleLoanStatus (per D-07). Projekt kompiluje się i UI działa poprawnie (potwierdzone przez checkpoint).
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/03-logistics-module/03-03-SUMMARY.md` z listą wszystkich zmian UI, potwierdzeniem auditowania i wynikiem checkpointu.
</output>
