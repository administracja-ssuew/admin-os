---
phase: 02-my-department-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - hooks/useLogisticsData.ts
  - hooks/useArchivingData.ts
  - hooks/useGrantsData.ts
autonomous: true
requirements:
  - REF-01

must_haves:
  truths:
    - "useLogisticsData zwraca { assets, loans, reports, members, loading, refetch } — żadnych zapytań Supabase w page.tsx dla pionu logistyki"
    - "useArchivingData zwraca { archiveFolders, petitions, members, loading, refetch }"
    - "useGrantsData zwraca { grants, members, loading, refetch }"
    - "Każdy hook akceptuje departmentId: string | undefined i nie fetchuje gdy undefined"
    - "Każdy hook ma stabilną referencję refetch (useCallback)"
  artifacts:
    - path: "hooks/useLogisticsData.ts"
      provides: "dane Supabase dla pionu logistyki"
      exports: ["useLogisticsData"]
    - path: "hooks/useArchivingData.ts"
      provides: "dane Supabase dla pionu archiwizacji"
      exports: ["useArchivingData"]
    - path: "hooks/useGrantsData.ts"
      provides: "dane Supabase dla pionu dotacji"
      exports: ["useGrantsData"]
  key_links:
    - from: "hooks/useLogisticsData.ts"
      to: "components/subcommittees/LogisticsPanel.tsx (plan 03)"
      via: "props: assets, loans, reports, members"
    - from: "hooks/useGrantsData.ts"
      to: "components/subcommittees/GrantsPanel.tsx (plan 03)"
      via: "props: grants, members"
---

<objective>
Wyodrębnij logikę fetchowania danych z my-department/page.tsx do trzech dedykowanych hooków.

Każdy hook enkapsuluje zapytania Supabase dla jednego pionu podkomisji i zwraca dane gotowe do przekazania jako props do paneli komponentów.

Purpose: page.tsx przestaje bezpośrednio używać supabase.from() dla danych podkomisji — cały fetching idzie przez hooki.
Output: 3 pliki hooków w hooks/, każdy ~50-80 linii z typami z types/index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/my-department/page.tsx
@hooks/useCurrentUser.ts
@hooks/useCases.ts
@types/index.ts

<interfaces>
<!-- Typy z 02-01 (types/index.ts po planie 01) -->
```typescript
export type DeptType = 'logistics' | 'archiving' | 'grants'

export interface Asset {
  id: string; name: string; asset_type: AssetType; status: AssetStatus
  location: string | null; notes: string | null; created_at: string
}

export interface EquipmentLoan {
  id: string; agreement_number: string | null; item_category: LoanItemCategory
  borrower_name: string; issue_date: string; return_date: string | null
  status: LoanStatus; notes: string | null; created_at: string
}

export interface Grant {
  id: string; signature: string | null; name: string; organizer: string | null
  type: GrantType; max_amount: number | null; scope: string; deadline: string | null
  status: GrantStatus; decision: GrantDecision; owner_id: string | null
  drive_link: string | null; description: string | null; notes: string | null
  created_at: string
  owner?: { first_name: string; last_name: string } | null
}

export interface ArchiveFolder {
  id: string; title: string; status: ArchiveFolderStatus; notes: string | null
  attachments: CaseAttachment[]; created_at: string
}

export interface Petition {
  id: string; title: string; recipient: string; submission_date: string
  status: PetitionStatus; attachments: CaseAttachment[]; created_at: string
}
```

<!-- Wzorzec hooka z useCases.ts -->
```typescript
// Wzorzec: useCallback + useEffect + realtime channel (opcjonalnie)
export function useCases(): UseCasesResult {
  const [cases, setCases] = useState<Case[]>([])
  const [loading, setLoading] = useState(true)
  const fetchCases = useCallback(async () => { ... }, [])
  useEffect(() => { fetchCases() }, [fetchCases])
  return { cases, loading, refetch: fetchCases }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utwórz useLogisticsData i useArchivingData</name>
  <files>
    hooks/useLogisticsData.ts
    hooks/useArchivingData.ts
  </files>
  <read_first>
    - hooks/useCurrentUser.ts (wzorzec 'use client' + useState + useEffect + useCallback)
    - app/my-department/page.tsx linie 125-139 (zapytania dla logistyki i archiwizacji)
    - app/my-department/page.tsx linie 39-64 (kształt stanów: assets, loans, reports, archiveFolders, petitions)
    - types/index.ts (Asset, EquipmentLoan, ArchiveFolder, Petition)
  </read_first>
  <action>
**hooks/useLogisticsData.ts**

Utwórz plik z sygnaturą:
```typescript
'use client'
import { useState, useEffect, useCallback } from 'react'
import { supabase } from '../lib/supabase'
import type { Asset, EquipmentLoan } from '../types'

interface DeptMember { id: string; first_name: string; last_name: string }

interface LogisticsReport {
  id: string; title: string; content: string | null
  submitted_by: string | null; submitted_at: string; status: string
  subcommittee_type: string
  submitted_by_user?: { first_name: string; last_name: string } | null
}

export interface UseLogisticsDataResult {
  assets: Asset[]
  loans: EquipmentLoan[]
  reports: LogisticsReport[]
  members: DeptMember[]
  loading: boolean
  refetch: () => Promise<void>
}

export function useLogisticsData(departmentId: string | undefined): UseLogisticsDataResult
```

Implementacja:
- `if (!departmentId) { setLoading(false); return }` na początku fetchData
- Zapytania (przenieś z page.tsx linie 126-131):
  - `supabase.from('assets').select('*').order('name', { ascending: true })`
  - `supabase.from('equipment_loans').select('*').order('issue_date', { ascending: false })`
  - `supabase.from('reports').select('*, submitted_by_user:users!reports_submitted_by_fkey(first_name, last_name)').eq('subcommittee_type', 'logistics').order('submitted_at', { ascending: false })`
  - `supabase.from('users').select('id, first_name, last_name').eq('department_id', departmentId)`
- useCallback dla fetchData, useEffect([fetchData])
- Zwróć `{ assets, loans, reports, members, loading, refetch: fetchData }`

---

**hooks/useArchivingData.ts**

Utwórz plik z sygnaturą:
```typescript
export interface UseArchivingDataResult {
  archiveFolders: ArchiveFolder[]
  petitions: Petition[]
  members: DeptMember[]
  loading: boolean
  refetch: () => Promise<void>
}

export function useArchivingData(departmentId: string | undefined): UseArchivingDataResult
```

Implementacja:
- `if (!departmentId) { setLoading(false); return }` na początku
- Zapytania (przenieś z page.tsx linie 134-139):
  - `supabase.from('archive_folders').select('*').order('created_at', { ascending: false })`
  - `supabase.from('petitions').select('*').order('submission_date', { ascending: false })`
  - `supabase.from('users').select('id, first_name, last_name').eq('department_id', departmentId)`
- Wzorzec identyczny z useLogisticsData
  </action>
  <verify>
    <automated>grep -n "export function useLogisticsData\|export function useArchivingData\|UseLogisticsDataResult\|UseArchivingDataResult" hooks/useLogisticsData.ts hooks/useArchivingData.ts</automated>
  </verify>
  <done>Oba pliki istnieją, eksportują funkcję hooka i interfejs result. `npx tsc --noEmit` bez błędów dla tych plików.</done>
</task>

<task type="auto">
  <name>Task 2: Utwórz useGrantsData</name>
  <files>hooks/useGrantsData.ts</files>
  <read_first>
    - app/my-department/page.tsx linie 120-123 (zapytanie grants_radar)
    - types/index.ts (Grant, GrantStatus, GrantDecision, GrantType)
    - hooks/useLogisticsData.ts (utwór przez Task 1 — dla spójności wzorca)
  </read_first>
  <action>
**hooks/useGrantsData.ts**

Sygnatura:
```typescript
'use client'
import { useState, useEffect, useCallback } from 'react'
import { supabase } from '../lib/supabase'
import type { Grant } from '../types'

interface DeptMember { id: string; first_name: string; last_name: string }

export interface UseGrantsDataResult {
  grants: Grant[]
  members: DeptMember[]
  loading: boolean
  refetch: () => Promise<void>
}

export function useGrantsData(departmentId: string | undefined): UseGrantsDataResult
```

Implementacja:
- `if (!departmentId) { setLoading(false); return }` na początku
- Zapytania (przenieś z page.tsx linia 121):
  - `supabase.from('grants_radar').select('*, owner:users!grants_radar_owner_id_fkey(first_name, last_name)').order('deadline', { ascending: true })`
  - `supabase.from('users').select('id, first_name, last_name').eq('department_id', departmentId)`
- useCallback dla fetchData, useEffect([fetchData])
- Zwróć `{ grants, members, loading, refetch: fetchData }`

Uwaga: fetchowanie grantów NIE jest filtrowane po department_id — tabela grants_radar jest globalna (przeznaczona dla całego pionu dotacji), co jest zgodne z obecną logiką w page.tsx.
  </action>
  <verify>
    <automated>grep -n "export function useGrantsData\|UseGrantsDataResult\|grants_radar" hooks/useGrantsData.ts</automated>
  </verify>
  <done>hooks/useGrantsData.ts eksportuje useGrantsData(departmentId) i UseGrantsDataResult. Zapytanie do grants_radar z join owner.</done>
</task>

</tasks>

<verification>
1. `grep -rn "from.*useLogisticsData\|from.*useArchivingData\|from.*useGrantsData" hooks/` — pliki istnieją
2. `npx tsc --noEmit 2>&1 | grep "useLogisticsData\|useArchivingData\|useGrantsData"` — brak błędów
3. Każdy hook akceptuje `departmentId: string | undefined` i nie crasha gdy undefined
</verification>

<success_criteria>
- 3 pliki hooków w hooks/, każdy ~50-80 linii
- Każdy zwraca typed result interface bez `any`
- Żaden hook nie importuje z app/my-department/page.tsx
- `npx tsc --noEmit` — zero błędów TypeScript
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-my-department-refactor/02-02-SUMMARY.md`
</output>
