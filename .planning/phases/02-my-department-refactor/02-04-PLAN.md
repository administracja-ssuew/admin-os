---
phase: 02-my-department-refactor
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - app/my-department/page.tsx
autonomous: true
requirements:
  - REF-03

must_haves:
  truths:
    - "app/my-department/page.tsx ma <=100 linii"
    - "page.tsx używa department.dept_type (enum) do routingu paneli — zero deptName.includes()"
    - "page.tsx nie zawiera żadnego supabase.from() poza wczytaniem użytkownika i departamentu"
    - "Trzy panele są renderowane warunkowo na podstawie dept_type"
    - "Wspólne elementy (nagłówek, zadania działu, notatka robocza) pozostają w page.tsx"
  artifacts:
    - path: "app/my-department/page.tsx"
      provides: "cienki orkiestrator tabów my-department"
      min_lines: 40
      max_lines: 100
      contains: "dept_type"
  key_links:
    - from: "app/my-department/page.tsx"
      to: "components/subcommittees/LogisticsPanel"
      via: "import { LogisticsPanel } from '../../components/subcommittees/LogisticsPanel'"
    - from: "app/my-department/page.tsx"
      to: "hooks/useLogisticsData"
      via: "import { useLogisticsData } from '../../hooks/useLogisticsData'"
    - from: "app/my-department/page.tsx"
      to: "department.dept_type"
      via: "switch(department.dept_type)"
---

<objective>
Odchudź app/my-department/page.tsx z 1320 linii do <=100 linii jako cienkiego orkiestratora.

page.tsx odpowiada teraz wyłącznie za: wczytanie current user + departamentu, wywołanie odpowiedniego hooka danych, warunkowe renderowanie odpowiedniego panelu na podstawie `department.dept_type`, sekcję nagłówka i ewentualnie wspólną notatkę roboczą.

Purpose: Cel fazy — page.tsx jako "cienki orkiestrator tabów bez bezpośrednich zapytań Supabase".
Output: Przepisany app/my-department/page.tsx, ~80 linii.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/my-department/page.tsx
@hooks/useCurrentUser.ts
@types/index.ts

<interfaces>
<!-- Hooki danych (plan 02) -->
```typescript
import { useLogisticsData } from '../../hooks/useLogisticsData'
import { useArchivingData } from '../../hooks/useArchivingData'
import { useGrantsData } from '../../hooks/useGrantsData'
// Każdy przyjmuje departmentId: string | undefined
// Zwraca { ..., loading, refetch }
```

<!-- Komponenty paneli (plan 03) -->
```typescript
import { LogisticsPanel } from '../../components/subcommittees/LogisticsPanel'
import { ArchivingPanel } from '../../components/subcommittees/ArchivingPanel'
import { GrantsPanel } from '../../components/subcommittees/GrantsPanel'
// Każdy przyjmuje: { ...data, members, currentUser, isAdmin, onRefetch }
```

<!-- Typ Department po planie 01 -->
```typescript
interface Department {
  id: string
  name: string
  dept_type: DeptType | null  // 'logistics' | 'archiving' | 'grants' | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Przepisz page.tsx jako orkiestrator</name>
  <files>app/my-department/page.tsx</files>
  <read_first>
    - app/my-department/page.tsx cały plik (zrozum co zostaje, co idzie do paneli)
    - hooks/useCurrentUser.ts (istniejący hook — można użyć zamiast inline session)
    - components/subcommittees/LogisticsPanel.tsx (props interface z planu 03)
    - components/subcommittees/ArchivingPanel.tsx (props interface z planu 03)
    - components/subcommittees/GrantsPanel.tsx (props interface z planu 03)
  </read_first>
  <action>
Zastąp CAŁĄ zawartość app/my-department/page.tsx nową implementacją. Docelowa struktura (~80 linii):

```typescript
'use client'

import { useState, useEffect } from 'react'
import { supabase } from '../../lib/supabase'
import Sidebar from '../../components/Sidebar'
import { Building2, Loader2 } from 'lucide-react'
import { useLogisticsData } from '../../hooks/useLogisticsData'
import { useArchivingData } from '../../hooks/useArchivingData'
import { useGrantsData } from '../../hooks/useGrantsData'
import { LogisticsPanel } from '../../components/subcommittees/LogisticsPanel'
import { ArchivingPanel } from '../../components/subcommittees/ArchivingPanel'
import { GrantsPanel } from '../../components/subcommittees/GrantsPanel'
import type { AppUser, Department } from '../../types'

export default function MyDepartmentPage() {
  const [loading, setLoading] = useState(true)
  const [currentUser, setCurrentUser] = useState<AppUser | null>(null)
  const [department, setDepartment] = useState<Department | null>(null)

  useEffect(() => {
    const loadUser = async () => {
      const { data: { session } } = await supabase.auth.getSession()
      if (!session?.user?.email) { setLoading(false); return }
      const { data } = await supabase
        .from('users')
        .select('*, departments(*)')
        .eq('email', session.user.email)
        .single()
      if (data) {
        setCurrentUser(data)
        setDepartment(data.departments ?? null)
      }
      setLoading(false)
    }
    loadUser()
  }, [])

  const isAdmin = currentUser?.system_role === 'admin' || currentUser?.system_role === 'superadmin'
  const deptId = department?.id

  // Wywołaj wszystkie trzy hooki bezwarunkowo (React rules of hooks)
  // Każdy hook sam sprawdza czy departmentId jest defined
  const logistics = useLogisticsData(department?.dept_type === 'logistics' ? deptId : undefined)
  const archiving = useArchivingData(department?.dept_type === 'archiving' ? deptId : undefined)
  const grants = useGrantsData(department?.dept_type === 'grants' ? deptId : undefined)

  if (loading) {
    return (
      <div className="flex items-center justify-center min-h-screen bg-slate-50 dark:bg-slate-950">
        <Loader2 size={32} className="animate-spin text-blue-500" />
      </div>
    )
  }

  const renderPanel = () => {
    if (!department) return (
      <div className="text-center text-slate-500 py-12">
        Nie jesteś przypisany do żadnego departamentu.
      </div>
    )
    switch (department.dept_type) {
      case 'logistics':
        return <LogisticsPanel {...logistics} currentUser={currentUser} isAdmin={isAdmin} onRefetch={logistics.refetch} />
      case 'archiving':
        return <ArchivingPanel {...archiving} currentUser={currentUser} isAdmin={isAdmin} onRefetch={archiving.refetch} />
      case 'grants':
        return <GrantsPanel {...grants} currentUser={currentUser} isAdmin={isAdmin} onRefetch={grants.refetch} />
      default:
        return (
          <div className="text-center text-slate-500 py-12">
            Brak dedykowanego widoku dla tego departamentu.
          </div>
        )
    }
  }

  return (
    <div className="flex h-screen bg-slate-50 dark:bg-slate-950 overflow-hidden">
      <Sidebar />
      <main className="flex-1 overflow-y-auto p-6 md:p-8">
        <div className="max-w-7xl mx-auto">
          <div className="mb-6 flex items-center gap-3">
            <Building2 size={28} className="text-blue-500" />
            <div>
              <h1 className="text-2xl font-extrabold text-slate-900 dark:text-white">
                {department?.name ?? 'Mój Departament'}
              </h1>
              {currentUser && (
                <p className="text-sm text-slate-500 dark:text-slate-400">
                  {currentUser.first_name} {currentUser.last_name}
                </p>
              )}
            </div>
          </div>
          {renderPanel()}
        </div>
      </main>
    </div>
  )
}
```

KRYTYCZNE:
- NIE przenoś z powrotem żadnej logiki mutacji — jest w panelach (plan 03)
- NIE przenoś żadnych stanów assets/loans/grants/archiveFolders — są w hookach (plan 02)
- NIE używaj deptName.includes() — tylko department.dept_type (enum z planu 01)
- Trzy hooki muszą być wywołane ZAWSZE (rules of hooks), nie warunkowo
- Rozkład propsów `{...logistics}` przekazuje assets, loans, reports, members, loading do panela

Jeśli wspólna sekcja zadań działu (deptTasks) była ważna — zachowaj ją jako opcjonalny blok przed renderPanel(), ale NIE jest wymagana dla spełnienia kryterium ~80 linii.
  </action>
  <verify>
    <automated>wc -l app/my-department/page.tsx && grep -n "dept_type\|useLogisticsData\|useArchivingData\|useGrantsData\|LogisticsPanel\|ArchivingPanel\|GrantsPanel" app/my-department/page.tsx</automated>
  </verify>
  <done>page.tsx ma <=100 linii. Zawiera import wszystkich 3 hooków i 3 paneli. Routing przez department.dept_type (switch). Zero wywołań supabase.from() poza blokiem loadUser.</done>
</task>

</tasks>

<verification>
1. `wc -l app/my-department/page.tsx` — wynik <=100
2. `grep -c "supabase.from" app/my-department/page.tsx` — wynik: 1 (tylko users select w loadUser)
3. `grep "deptName.includes\|department.name.toLowerCase" app/my-department/page.tsx` — brak wyników
4. `npx tsc --noEmit 2>&1 | grep "my-department"` — zero błędów
5. `npx next build 2>&1 | tail -5` — build kompiluje się bez błędów
</verification>

<success_criteria>
- page.tsx: <=100 linii
- Routing przez `department.dept_type` (enum), nie string matching
- Zero bezpośrednich supabase.from().select() w page.tsx (poza loadUser)
- `npx next build` — sukces
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-my-department-refactor/02-04-SUMMARY.md`
</output>
