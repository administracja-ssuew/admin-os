---
phase: 02-my-department-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260404_add_dept_type_enum.sql
  - types/index.ts
autonomous: true
requirements:
  - REF-04
  - REF-05

must_haves:
  truths:
    - "Kolumna dept_type istnieje w tabeli departments z wartościami: logistics | archiving | grants"
    - "Istniejące wiersze departments mają wypełnione dept_type (backfill przez nazwy)"
    - "Typy TypeScript EquipmentLoan, Grant, EligibilityCriterion, MeetingProtocol są wyeksportowane z types/index.ts"
    - "Typ Department ma pole dept_type: DeptType | null"
  artifacts:
    - path: "supabase/migrations/20260404_add_dept_type_enum.sql"
      provides: "migracja enum dept_type + backfill + constraint"
      contains: "CREATE TYPE dept_type_enum"
    - path: "types/index.ts"
      provides: "interfejsy domenowe dla phase-2"
      exports:
        - EquipmentLoan
        - Grant
        - EligibilityCriterion
        - MeetingProtocol
        - DeptType
  key_links:
    - from: "types/index.ts"
      to: "hooks/useLogisticsData.ts (plan 02)"
      via: "import { EquipmentLoan, Asset } from '../types'"
    - from: "supabase/migrations/20260404_add_dept_type_enum.sql"
      to: "app/my-department/page.tsx (plan 04)"
      via: "department.dept_type zamiast deptName.includes()"
---

<objective>
Fundament refaktoru: migracja bazy danych i nowe typy TypeScript.

Plan dodaje kolumnę `dept_type` (typ wyliczeniowy) do tabeli `departments` oraz backfilluje istniejące wiersze na podstawie nazw działów. Jednocześnie rozszerza `types/index.ts` o brakujące interfejsy domenowe wymagane przez hooki i panele w kolejnych planach.

Purpose: Hooki danych i komponenty paneli w planach 02-04 potrzebują stabilnych typów i enum do routingu — to musi istnieć jako pierwsze.
Output: Plik migracji SQL gotowy do `supabase db push`, rozszerzony types/index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/my-department/page.tsx
@types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migracja SQL — enum dept_type + backfill</name>
  <files>supabase/migrations/20260404_add_dept_type_enum.sql</files>
  <read_first>
    - supabase/migrations/20260404_stab_data_integrity.sql (wzorzec migracji w projekcie)
    - app/my-department/page.tsx linie 118-139 (obecna logika rozpoznawania pionu: deptName.includes('dotacj'), 'logistyk', 'archiwizacj')
  </read_first>
  <action>
Utwórz plik migracji SQL zawierający dokładnie te sekcje:

1. Utwórz typ enum (jeśli nie istnieje):
```sql
CREATE TYPE IF NOT EXISTS dept_type_enum AS ENUM ('logistics', 'archiving', 'grants');
```

2. Dodaj kolumnę do tabeli departments (idempotentnie):
```sql
ALTER TABLE departments
  ADD COLUMN IF NOT EXISTS dept_type dept_type_enum;
```

3. Backfill na podstawie fragmentów polskich nazw (te same heurystyki co w page.tsx):
```sql
UPDATE departments SET dept_type = 'grants'
  WHERE dept_type IS NULL AND lower(name) LIKE '%dotacj%';

UPDATE departments SET dept_type = 'logistics'
  WHERE dept_type IS NULL AND (lower(name) LIKE '%logistyk%' OR lower(name) LIKE '%logitech%');

UPDATE departments SET dept_type = 'archiving'
  WHERE dept_type IS NULL AND (lower(name) LIKE '%archiwizacj%' OR lower(name) LIKE '%bieżąc%');
```

4. Dodaj komentarz i indeks:
```sql
COMMENT ON COLUMN departments.dept_type IS 'Enum type for subcommittee routing. NULL = generic department.';
CREATE INDEX IF NOT EXISTS departments_dept_type_idx ON departments (dept_type);
```

NIE dodawaj NOT NULL constraint — działy ogólne (brak podkomisji) mogą mieć NULL.
NIE modyfikuj RLS — nie ma potrzeby.
  </action>
  <verify>
    <automated>grep -n "CREATE TYPE\|dept_type_enum\|ENUM\|backfill\|UPDATE departments" supabase/migrations/20260404_add_dept_type_enum.sql | head -20</automated>
  </verify>
  <done>Plik migracji zawiera CREATE TYPE dept_type_enum, ADD COLUMN IF NOT EXISTS dept_type, trzy UPDATE (backfill), indeks.</done>
</task>

<task type="auto">
  <name>Task 2: Rozszerz types/index.ts o interfejsy domenowe</name>
  <files>types/index.ts</files>
  <read_first>
    - types/index.ts (cały plik — istniejące interfejsy, aby nie duplikować)
    - app/my-department/page.tsx linie 40-77 (stany: loanForm, assetForm, grantForm, archiveForm — kształt danych)
  </read_first>
  <action>
Dopisz na końcu pliku `types/index.ts` (po istniejących interfejsach, z separatorem komentarza) dokładnie te definicje:

```typescript
// ─── TYPY MY-DEPARTMENT (Phase 2 Refactor) ────────────────────

export type DeptType = 'logistics' | 'archiving' | 'grants'

// Zaktualizuj istniejący interfejs Department — dodaj pole dept_type:
// (usuń stary export interface Department i zastąp poniższym)
// UWAGA: Department jest zdefiniowany ~linia 140 — zmień tylko to pole
export interface Department {
  id: string
  name: string
  dept_type: DeptType | null
}

export type AssetStatus = 'available' | 'low_stock' | 'maintenance'
export type AssetType = 'Artykuły biurowe' | 'Sprzęt IT' | 'Meble' | 'Audio-Video' | 'Inne'

export interface Asset {
  id: string
  name: string
  asset_type: AssetType
  status: AssetStatus
  location: string | null
  notes: string | null
  created_at: string
}

export type LoanStatus = 'Wypożyczone' | 'Zwrócone'
export type LoanItemCategory = 'Namiot Plenerowy' | 'Sprzęt Audio' | 'Projektor' | 'Meble' | 'Inne'

export interface EquipmentLoan {
  id: string
  agreement_number: string | null
  item_category: LoanItemCategory
  borrower_name: string
  issue_date: string
  return_date: string | null
  status: LoanStatus
  notes: string | null
  created_at: string
}

export type GrantStatus = 'RADAR' | 'W TOKU' | 'ARCHIWUM'
export type GrantDecision = 'OCZEKUJE' | 'ZAAKCEPTOWANE' | 'ODRZUCONE'
export type GrantType = 'DOTACJA' | 'PATRONAT'

export interface Grant {
  id: string
  signature: string | null
  name: string
  organizer: string | null
  type: GrantType
  max_amount: number | null
  scope: string
  deadline: string | null
  status: GrantStatus
  decision: GrantDecision
  owner_id: string | null
  drive_link: string | null
  description: string | null
  notes: string | null
  created_at: string
  // relacje
  owner?: { first_name: string; last_name: string } | null
}

export type ArchiveFolderStatus = 'W przygotowaniu' | 'Aktywna' | 'Zamknięta'

export interface ArchiveFolder {
  id: string
  title: string
  status: ArchiveFolderStatus
  notes: string | null
  attachments: CaseAttachment[]
  created_at: string
}

export type PetitionStatus = 'Złożone' | 'Rozpatrzone' | 'Odrzucone'

export interface Petition {
  id: string
  title: string
  recipient: string
  submission_date: string
  status: PetitionStatus
  attachments: CaseAttachment[]
  created_at: string
}

export interface EligibilityCriterion {
  id: string
  grant_id: string
  description: string
  is_met: boolean | null
}

export interface MeetingProtocol {
  id: string
  meeting_id: string
  content: string
  status: ProtocolStatus
  created_at: string
}
```

WAŻNE: Interfejs `Department` jest już w pliku (~linia 140). Znajdź go i zaktualizuj (dodaj `dept_type: DeptType | null`), NIE duplikuj. Pozostałe typy dopisz na końcu w sekcji komentarza.

`CaseAttachment` i `ProtocolStatus` już istnieją w pliku — użyj ich bezpośrednio.
  </action>
  <verify>
    <automated>grep -n "DeptType\|EquipmentLoan\|Grant\b\|ArchiveFolder\|Petition\b\|EligibilityCriterion\|MeetingProtocol\|dept_type" types/index.ts</automated>
  </verify>
  <done>types/index.ts eksportuje: DeptType, Asset, EquipmentLoan, Grant, ArchiveFolder, Petition, EligibilityCriterion, MeetingProtocol. Interfejs Department ma pole dept_type: DeptType | null. Brak duplikatów istniejących typów.</done>
</task>

</tasks>

<verification>
1. `grep "dept_type_enum" supabase/migrations/20260404_add_dept_type_enum.sql` — zwraca wynik
2. `grep "EquipmentLoan\|Grant\b\|DeptType" types/index.ts` — zwraca wynik
3. `npx tsc --noEmit 2>&1 | head -20` — zero błędów TypeScript w types/index.ts
</verification>

<success_criteria>
- Plik migracji zawiera kompletny SQL gotowy do uruchomienia w Supabase
- types/index.ts nie ma duplikatów; Department.dept_type istnieje
- `npx tsc --noEmit` nie zgłasza błędów dla types/
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-my-department-refactor/02-01-SUMMARY.md`
</output>
