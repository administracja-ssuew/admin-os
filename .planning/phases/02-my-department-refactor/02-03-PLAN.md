---
phase: 02-my-department-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - components/subcommittees/LogisticsPanel.tsx
  - components/subcommittees/ArchivingPanel.tsx
  - components/subcommittees/GrantsPanel.tsx
autonomous: true
requirements:
  - REF-02

must_haves:
  truths:
    - "LogisticsPanel renderuje tabele assets + loans + raporty; przyjmuje props z useLogisticsData"
    - "ArchivingPanel renderuje teczki archiwalne + petycje + upload plików; przyjmuje props z useArchivingData"
    - "GrantsPanel renderuje radar dotacji + szufladę grantu + modal dodawania; przyjmuje props z useGrantsData"
    - "Każdy panel jest 'use client' i nie zawiera bezpośrednich wywołań supabase.from() (mutacje przez propsy callback)"
    - "Typy propsów paneli używają interfejsów z types/index.ts (zero `any`)"
  artifacts:
    - path: "components/subcommittees/LogisticsPanel.tsx"
      provides: "widok UI pionu logistyki"
      exports: ["LogisticsPanel"]
    - path: "components/subcommittees/ArchivingPanel.tsx"
      provides: "widok UI pionu archiwizacji + upload"
      exports: ["ArchivingPanel"]
    - path: "components/subcommittees/GrantsPanel.tsx"
      provides: "widok UI pionu dotacji + szuflada"
      exports: ["GrantsPanel"]
  key_links:
    - from: "components/subcommittees/LogisticsPanel.tsx"
      to: "app/my-department/page.tsx (plan 04)"
      via: "import { LogisticsPanel } from '../../components/subcommittees/LogisticsPanel'"
    - from: "components/subcommittees/GrantsPanel.tsx"
      to: "app/my-department/page.tsx (plan 04)"
      via: "import { GrantsPanel } from '../../components/subcommittees/GrantsPanel'"
---

<objective>
Przenieś JSX i logikę mutacji z my-department/page.tsx do trzech komponentów paneli per-podkomisja.

Każdy panel otrzymuje dane przez props (z hooków) i callbacks mutacji. Komponenty NIE fetchują danych samodzielnie — to czysty podział: hooki = dane, panele = UI.

Purpose: Redukuje page.tsx z 1320 linii do ~80 (plan 04). Każdy panel jest izolowaną jednostką.
Output: 3 pliki komponentów w components/subcommittees/, każdy ~200-350 linii.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/my-department/page.tsx
@types/index.ts
@components/ConfirmDialog.tsx

<interfaces>
<!-- Props paneli — kontrakt dla page.tsx w planie 04 -->

**LogisticsPanelProps:**
```typescript
interface LogisticsPanelProps {
  assets: Asset[]
  loans: EquipmentLoan[]
  reports: LogisticsReport[]
  members: DeptMember[]
  currentUser: AppUser | null
  isAdmin: boolean
  onRefetch: () => Promise<void>
}
```

**ArchivingPanelProps:**
```typescript
interface ArchivingPanelProps {
  archiveFolders: ArchiveFolder[]
  petitions: Petition[]
  members: DeptMember[]
  currentUser: AppUser | null
  isAdmin: boolean
  onRefetch: () => Promise<void>
}
```

**GrantsPanelProps:**
```typescript
interface GrantsPanelProps {
  grants: Grant[]
  members: DeptMember[]
  currentUser: AppUser | null
  isAdmin: boolean
  onRefetch: () => Promise<void>
}
```

<!-- Typy z types/index.ts (plan 01) -->
```typescript
import type { Asset, EquipmentLoan, Grant, ArchiveFolder, Petition } from '../../types'
import type { AppUser } from '../../types'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utwórz LogisticsPanel.tsx i ArchivingPanel.tsx</name>
  <files>
    components/subcommittees/LogisticsPanel.tsx
    components/subcommittees/ArchivingPanel.tsx
  </files>
  <read_first>
    - app/my-department/page.tsx linie 125-357 (logika mutacji: handleAddAsset, handleAddLoan, toggleLoanStatus, updateAssetStatus, handleAddReport, updateReportStatus, deleteAsset, deleteLoan, getTimelineData)
    - app/my-department/page.tsx linie 440-900 (JSX dla pionu logistyki — tabela assets, tabela loans, wykresy)
    - app/my-department/page.tsx linie 134-236 (logika mutacji AiKB: handleAddArchiveFolder, handleAddPetition, deleteArchiveFolder, deletePetition, handleAiKBUpload, updateArchiveStatus, updatePetitionStatus)
    - app/my-department/page.tsx linie 900-1085 (JSX dla pionu archiwizacji)
    - components/ConfirmDialog.tsx (interfejs props)
    - hooks/useLogisticsData.ts (typy UseLogisticsDataResult — z planu 02)
  </read_first>
  <action>
**Utwórz katalog:** `components/subcommittees/` (jeśli nie istnieje)

**components/subcommittees/LogisticsPanel.tsx:**

Plik zaczyna się od `'use client'`. Eksportuje domyślnie `export function LogisticsPanel(props: LogisticsPanelProps)`.

Przenieś z page.tsx:
- Stany lokalne: isAssetModalOpen, assetForm, isLoanModalOpen, loanForm, isReportModalOpen, reportForm, assetTypeFilter, assetStatusFilter, loanStatusFilter, confirmDelete
- Stany szuflad: selectedFolder (nie dotyczy logistyki), stan drilldownu aktywów
- Funkcje mutacji: handleAddAsset, handleAddLoan, toggleLoanStatus, updateAssetStatus, deleteAsset, deleteLoan, handleAddReport, updateReportStatus, getTimelineData
- Cały JSX dla logistyki (tabele assets, loans, raporty, modale, ConfirmDialog)

Mutacje MUSZĄ na końcu wywoływać `props.onRefetch()` zamiast `fetchDepartmentData()`.
Import ConfirmDialog z `'../ConfirmDialog'`.
Import supabase z `'../../lib/supabase'` (mutacje lokalne są OK, fetching jest w hooku).
Importy ikon z `'lucide-react'`.
Import logAudit z `'../../lib/audit'` (dla deleteAsset, deleteLoan).

---

**components/subcommittees/ArchivingPanel.tsx:**

Plik zaczyna się od `'use client'`. Eksportuje `export function ArchivingPanel(props: ArchivingPanelProps)`.

Przenieś z page.tsx:
- Stany: isArchiveModalOpen, archiveForm, isFolderDrawerOpen, selectedFolder, isPetitionModalOpen, petitionForm, isPetitionDrawerOpen, selectedPetition, isUploading
- Funkcje: handleAddArchiveFolder, updateArchiveStatus, deleteArchiveFolder, handleAddPetition, updatePetitionStatus, deletePetition, handleAiKBUpload
- Cały JSX dla archiwizacji (teczki, petycje, upload, szuflady boczne, modale)

handleAiKBUpload przenieś kompletnie (wraz z logiką odpolszczacza i uploadu do storage `adminos-files`).
Mutacje wywołują `props.onRefetch()` zamiast `fetchDepartmentData()`.
  </action>
  <verify>
    <automated>grep -n "export function LogisticsPanel\|export function ArchivingPanel\|LogisticsPanelProps\|ArchivingPanelProps" components/subcommittees/LogisticsPanel.tsx components/subcommittees/ArchivingPanel.tsx</automated>
  </verify>
  <done>Oba pliki eksportują named function component z typed props. Katalog components/subcommittees/ istnieje. Brak `fetchDepartmentData` wewnątrz paneli.</done>
</task>

<task type="auto">
  <name>Task 2: Utwórz GrantsPanel.tsx</name>
  <files>components/subcommittees/GrantsPanel.tsx</files>
  <read_first>
    - app/my-department/page.tsx linie 317-332 (mutacje: handleAddGrant, updateGrantStatus, updateGrantDecision, deleteGrant)
    - app/my-department/page.tsx linie 366-440 (JSX statystyki grantów)
    - app/my-department/page.tsx linie 440-560 (tabela grantów + filtry)
    - app/my-department/page.tsx linie 1131-1228 (modal nowej dotacji + szuflada grantu)
    - types/index.ts (Grant, GrantStatus, GrantDecision, GrantType)
  </read_first>
  <action>
**components/subcommittees/GrantsPanel.tsx:**

Plik zaczyna się od `'use client'`. Eksportuje `export function GrantsPanel(props: GrantsPanelProps)`.

Przenieś z page.tsx:
- Stany lokalne: isGrantModalOpen, isSubmittingGrant, grantForm, selectedGrant, isGrantDrawerOpen, grantSearch, grantStatusFilter, grantTypeFilter, confirmDelete
- Funkcje mutacji: handleAddGrant, updateGrantStatus, updateGrantDecision, deleteGrant
- Cały JSX: statystyki (3 karty), tabela z filtrami, modal nowej dotacji/patronatu, szuflada grantu, ConfirmDialog

Stany filtrowania: grantSearch, grantStatusFilter, grantTypeFilter — zostają lokalne w komponencie.
Logika filtrowania: `filteredGrants` obliczana z `props.grants` (nie ze stanu).
deleteGrant wywołuje `props.onRefetch()` i używa `props.currentUser?.id` dla logAudit.
Import ConfirmDialog z `'../ConfirmDialog'`.
Import logAudit z `'../../lib/audit'`.
Import supabase z `'../../lib/supabase'` (mutacje).
  </action>
  <verify>
    <automated>grep -n "export function GrantsPanel\|GrantsPanelProps\|grants_radar\|isGrantModalOpen\|filteredGrants" components/subcommittees/GrantsPanel.tsx</automated>
  </verify>
  <done>components/subcommittees/GrantsPanel.tsx eksportuje GrantsPanel z props.grants (typed Grant[]). Filtrowanie oparte na props.grants, nie wewnętrznym stanie. Brak supabase.from('grants_radar').select wewnątrz komponentu.</done>
</task>

</tasks>

<verification>
1. `ls components/subcommittees/` — 3 pliki: LogisticsPanel.tsx, ArchivingPanel.tsx, GrantsPanel.tsx
2. `grep -rn "fetchDepartmentData" components/subcommittees/` — brak wyników (zero bezpośrednich fetchów)
3. `grep -rn "supabase.from.*select" components/subcommittees/` — brak wyników (panele nie fetchują)
4. `npx tsc --noEmit 2>&1 | grep "subcommittees"` — brak błędów TypeScript
</verification>

<success_criteria>
- 3 pliki komponentów w components/subcommittees/
- Żaden panel nie wywołuje supabase.from().select() — tylko .update()/.insert()/.delete()
- Wszystkie props są typowane (zero `any` w interfejsach props)
- `npx tsc --noEmit` — zero błędów
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/02-my-department-refactor/02-03-SUMMARY.md`
</output>
