---
phase: 04-archiving-module
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - app/meetings/page.tsx
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03

must_haves:
  truths:
    - "Drawer protokołu zawiera sekcję upload pliku z przyciskiem dołączania dokumentu"
    - "Po uploadzie plik widoczny jest jako link do pobrania przy protokole"
    - "Przycisk 'Zablokuj protokół' pojawia się tylko dla protokołów draft i tylko w drawerze"
    - "Po kliknięciu 'Zablokuj' protocol_status zmienia się na 'finalized', formularz jest disabled, przycisk znika"
    - "Zablokowanego protokołu nie można odblokować (brak przycisku Odblokuj — per D-03)"
  artifacts:
    - path: "app/meetings/page.tsx"
      provides: "Upload pliku + finalizacja protokołu"
      contains: "handleFileUpload|handleFinalize|finalized"
      min_lines: 200
  key_links:
    - from: "handleFileUpload"
      to: "supabase.storage.from('adminos-files')"
      via: "upload do bucketu adminos-files, ścieżka: protocols/{protocolId}/{uuid}/{fileName}"
      pattern: "adminos-files.*protocols"
    - from: "handleFinalize"
      to: "supabase.from('meeting_protocols').update"
      via: "update protocol_status = 'finalized'"
      pattern: "protocol_status.*finalized"
---

<objective>
Rozszerzyć stronę /meetings o upload pliku do protokołu oraz przycisk finalizacji (blokowania) protokołu.

Purpose: ARCH-02 (upload pliku) i ARCH-03 (blokowanie protokołu). Plan buduje na 04-02 — drawer protokołu istnieje, dodajemy nowe sekcje wewnątrz niego. Per D-03: po zablokowaniu formularz disabled + badge "Zablokowany", przycisk znika, brak możliwości odblokowania.
Output: Kompletny drawer z uploadem pliku i przyciskiem finalizacji.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-archiving-module/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/04-archiving-module/04-02-SUMMARY.md
@types/index.ts

<interfaces>
<!-- Wzorzec upload pliku z ArchivingPanel.tsx (ten sam bucket i pattern odpolszczacza) -->

```typescript
// Wzorzec upload z ArchivingPanel.tsx — REUSE:
const safeFileName = file.name
  .replace(/ą/g, 'a').replace(/Ą/g, 'A')
  .replace(/ć/g, 'c').replace(/Ć/g, 'C')
  .replace(/ę/g, 'e').replace(/Ę/g, 'E')
  .replace(/ł/g, 'l').replace(/Ł/g, 'L')
  .replace(/ń/g, 'n').replace(/Ń/g, 'N')
  .replace(/ó/g, 'o').replace(/Ó/g, 'O')
  .replace(/ś/g, 's').replace(/Ś/g, 'S')
  .replace(/ź/g, 'z').replace(/Ź/g, 'Z')
  .replace(/ż/g, 'z').replace(/Ż/g, 'Z')
  .replace(/[^a-zA-Z0-9.\-_]/g, '_')

const filePath = `protocols/${protocolId}/${crypto.randomUUID()}/${safeFileName}`

const { error: uploadError } = await supabase.storage
  .from('adminos-files')
  .upload(filePath, file, { contentType: file.type })

const { data } = supabase.storage.from('adminos-files').getPublicUrl(filePath)
// data.publicUrl -> zapisz jako file_url, file.name jako file_name w rekordzie
```

```typescript
// Mutacja finalizacji:
const { error } = await supabase.from('meeting_protocols')
  .update({ protocol_status: 'finalized', updated_at: new Date().toISOString() })
  .eq('id', protocolId)
```

```typescript
// Interfejs MeetingProtocol (z 04-01):
export interface MeetingProtocol {
  id: string
  title: string
  date: string
  participants: string
  agenda: string
  findings: string
  actions: string
  protocol_status: ProtocolStatus  // 'draft' | 'finalized'
  file_url: string | null
  file_name: string | null
  created_by: string | null
  created_at: string
  updated_at: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upload pliku i finalizacja protokołu w drawerze</name>
  <files>app/meetings/page.tsx</files>
  <action>
Przeczytaj aktualny app/meetings/page.tsx (z 04-02). Dodaj do komponentu MeetingsPage:

NOWE STANY (dołącz do istniejących):
```typescript
const [isUploading, setIsUploading] = useState(false)
const [isFinalizing, setIsFinalizing] = useState(false)
```

NOWA FUNKCJA handleFileUpload (dodaj po handleSaveEdit):
```typescript
const handleFileUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0]
  if (!file || !selectedProtocol) return
  setIsUploading(true)
  const toastId = toast.loading('Wrzucanie pliku...')
  try {
    const safeFileName = file.name
      .replace(/ą/g, 'a').replace(/Ą/g, 'A')
      .replace(/ć/g, 'c').replace(/Ć/g, 'C')
      .replace(/ę/g, 'e').replace(/Ę/g, 'E')
      .replace(/ł/g, 'l').replace(/Ł/g, 'L')
      .replace(/ń/g, 'n').replace(/Ń/g, 'N')
      .replace(/ó/g, 'o').replace(/Ó/g, 'O')
      .replace(/ś/g, 's').replace(/Ś/g, 'S')
      .replace(/ź/g, 'z').replace(/Ź/g, 'Z')
      .replace(/ż/g, 'z').replace(/Ż/g, 'Z')
      .replace(/[^a-zA-Z0-9.\-_]/g, '_')
    const filePath = `protocols/${selectedProtocol.id}/${crypto.randomUUID()}/${safeFileName}`
    const { error: uploadError } = await supabase.storage
      .from('adminos-files')
      .upload(filePath, file, { contentType: file.type })
    if (uploadError) throw uploadError
    const { data: urlData } = supabase.storage.from('adminos-files').getPublicUrl(filePath)
    const { error: updateError } = await supabase.from('meeting_protocols')
      .update({ file_url: urlData.publicUrl, file_name: file.name, updated_at: new Date().toISOString() })
      .eq('id', selectedProtocol.id)
    if (updateError) throw updateError
    toast.success('Plik dołączony!', { id: toastId })
    setSelectedProtocol({ ...selectedProtocol, file_url: urlData.publicUrl, file_name: file.name })
    await fetchProtocols()
  } catch {
    toast.error('Błąd podczas wgrywania pliku', { id: toastId })
  } finally {
    setIsUploading(false)
  }
}
```

NOWA FUNKCJA handleFinalize (dodaj po handleFileUpload):
```typescript
const handleFinalize = async () => {
  if (!selectedProtocol || selectedProtocol.protocol_status === 'finalized') return
  setIsFinalizing(true)
  const toastId = toast.loading('Blokowanie protokołu...')
  const { error } = await supabase.from('meeting_protocols')
    .update({ protocol_status: 'finalized', updated_at: new Date().toISOString() })
    .eq('id', selectedProtocol.id)
  if (!error) {
    toast.success('Protokół zablokowany!', { id: toastId })
    setSelectedProtocol({ ...selectedProtocol, protocol_status: 'finalized' })
    await fetchProtocols()
  } else {
    toast.error('Błąd podczas blokowania', { id: toastId })
  }
  setIsFinalizing(false)
}
```

W JSX DRAWERA — dodaj po sekcji pól formularza, przed przyciskiem "Zapisz zmiany":

SEKCJA PLIKU:
```tsx
{/* Plik protokołu */}
<div className="border-t border-slate-200 dark:border-slate-700 pt-4 mt-4">
  <p className="text-sm font-bold text-slate-700 dark:text-slate-300 mb-2">Plik protokołu</p>
  {selectedProtocol?.file_url ? (
    <a
      href={selectedProtocol.file_url}
      target="_blank"
      rel="noopener noreferrer"
      className="flex items-center gap-2 text-blue-600 dark:text-blue-400 text-sm hover:underline"
    >
      <Paperclip size={14} />
      {selectedProtocol.file_name ?? 'Plik protokołu'}
    </a>
  ) : (
    <p className="text-xs text-slate-500 dark:text-slate-400">Brak dołączonego pliku</p>
  )}
  {!isFinalized && (
    <label className="mt-2 flex items-center gap-2 cursor-pointer text-sm text-slate-600 dark:text-slate-400 hover:text-blue-600 dark:hover:text-blue-400">
      <UploadCloud size={16} />
      {isUploading ? 'Wgrywanie...' : 'Dołącz plik'}
      <input type="file" className="hidden" onChange={handleFileUpload} disabled={isUploading} />
    </label>
  )}
</div>
```

PRZYCISK FINALIZACJI (dodaj na końcu drawera, przed zamknięciem):
```tsx
{!isFinalized && (
  <div className="border-t border-slate-200 dark:border-slate-700 pt-4 mt-4">
    <button
      onClick={handleFinalize}
      disabled={isFinalizing}
      className="w-full px-4 py-2 bg-red-600 hover:bg-red-700 text-white font-bold rounded-lg transition-colors flex items-center justify-center gap-2 disabled:opacity-50"
    >
      <Lock size={16} />
      {isFinalizing ? 'Blokowanie...' : 'Zablokuj protokół'}
    </button>
    <p className="text-xs text-slate-500 dark:text-slate-400 mt-2 text-center">
      Po zablokowaniu protokół nie będzie mógł być edytowany.
    </p>
  </div>
)}
```

`isFinalized` to stała w bloku renderowania drawera:
```typescript
const isFinalized = selectedProtocol?.protocol_status === 'finalized'
```

Upewnij się że Paperclip, UploadCloud, Lock są importowane z lucide-react.
  </action>
  <verify>
    <automated>grep -c "handleFileUpload" app/meetings/page.tsx && grep -c "handleFinalize" app/meetings/page.tsx && grep -c "adminos-files.*protocols\|protocols.*adminos-files" app/meetings/page.tsx</automated>
  </verify>
  <done>Drawer protokołu zawiera: sekcję pliku z linkiem do pobrania lub przycisk upload, przycisk "Zablokuj protokół" widoczny tylko dla draft. Po kliknięciu Zablokuj protocol_status zmienia się na 'finalized', pola stają się disabled, przycisk znika.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — brak nowych błędów TypeScript
- `grep "handleFileUpload\|handleFinalize" app/meetings/page.tsx` — obie funkcje istnieją
- `grep "protocol_status.*finalized" app/meetings/page.tsx` — update na finalized istnieje
- `grep "adminos-files" app/meetings/page.tsx` — bucket adminos-files używany
- `grep "disabled.*isFinalized\|isFinalized.*disabled" app/meetings/page.tsx` — pola disabled
- Wizualnie: drawer protokołu draft pokazuje sekcję upload + przycisk Zablokuj
</verification>

<success_criteria>
- Upload pliku do protokołu zapisuje file_url i file_name w rekordzie
- Kliknięcie "Zablokuj protokół" ustawia protocol_status = 'finalized'
- Pola formularza i przycisk Zablokuj są niedostępne dla finalized
- Brak błędów TypeScript
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/04-archiving-module/04-03-SUMMARY.md` z:
- Potwierdzeniem działania upload (bucket: adminos-files, ścieżka: protocols/{id}/...)
- Potwierdzeniem działania finalizacji
- Notą o tym że /meetings jest w pełni gotowe (ARCH-01, ARCH-02, ARCH-03 spełnione)
</output>
