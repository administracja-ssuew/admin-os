---
phase: 04-archiving-module
plan: 05
type: execute
wave: 3
depends_on:
  - 04-04
files_modified:
  - components/subcommittees/ArchivingPanel.tsx
  - hooks/useArchivingData.ts
autonomous: true
requirements:
  - ARCH-05
  - ARCH-06

must_haves:
  truths:
    - "Każdy folder archiwum ma badge z typem: szary 'Ogólny' lub niebieski 'Raport projektowy'"
    - "Formularz tworzenia folderu zawiera wybór typu (select: ogólny / raport projektowy)"
    - "Zmiana statusu folderu wywołuje logAudit() z entityType='archive_folder'"
    - "useArchivingData pobiera pole folder_type z bazy danych"
    - "Istniejąca funkcjonalność folderów (tworzenie, usuwanie, upload) pozostaje niezmieniona"
  artifacts:
    - path: "components/subcommittees/ArchivingPanel.tsx"
      provides: "Badge folder_type + audit log przy zmianie statusu"
      contains: "folder_type|logAudit|FolderType"
    - path: "hooks/useArchivingData.ts"
      provides: "Pobieranie folder_type z archive_folders"
      contains: "folder_type"
  key_links:
    - from: "updateArchiveStatus w ArchivingPanel"
      to: "logAudit() z lib/audit.ts"
      via: "wywołanie po update status, entityType='archive_folder'"
      pattern: "logAudit.*archive_folder"
    - from: "badge folder_type w JSX"
      to: "folder.folder_type"
      via: "conditional Tailwind classes: general=szary, project_report=niebieski"
      pattern: "folder_type.*general|general.*folder_type"
---

<objective>
Dodać typ folderu (badge i formularz) oraz audit log przy zmianie statusu folderu archiwum.

Purpose: ARCH-05 (typy folderów z badge) i ARCH-06 (audit log zmiany statusu). Per D-05: ogólny = szary badge, raport projektowy = niebieski badge. Per D-06: logAudit() ten sam wzorzec co LOG-05 z Fazy 3.
Output: Foldery mają wizualny typ, zmiany statusu są rejestrowane w audit logu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-archiving-module/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/04-archiving-module/04-04-SUMMARY.md
@types/index.ts

<interfaces>
<!-- Interfejsy wymagane -->

```typescript
// FolderType i ArchiveFolder (z types/index.ts po 04-01):
export type FolderType = 'general' | 'project_report'

export interface ArchiveFolder {
  id: string
  title: string
  status: ArchiveFolderStatus
  folder_type: FolderType          // Dodane w 04-01
  notes: string | null
  attachments: CaseAttachment[]
  created_at: string
}
```

```typescript
// logAudit (lib/audit.ts):
import { logAudit } from '../../lib/audit'

await logAudit({
  userId: currentUser.id,
  action: 'STATUS_CHANGED',
  entityType: 'archive_folder',
  entityId: id,
  oldValue: { status: oldStatus },
  newValue: { status: newStatus },
})
```

```typescript
// useArchivingData (hooks/useArchivingData.ts) — query archive_folders:
// Obecny select: '*' (wszystkie kolumny) — wystarczy jeśli folder_type jest w tabeli po migracji
// Interfejs ArchiveFolder po 04-01 zawiera folder_type — nic do zmiany jeśli select('*')
```

Badge wzorzec (per D-05):
- general: `px-2 py-0.5 rounded-full text-xs font-bold bg-slate-100 dark:bg-slate-700 text-slate-600 dark:text-slate-300`
- project_report: `px-2 py-0.5 rounded-full text-xs font-bold bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-400`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: folder_type w useArchivingData i badge w ArchivingPanel</name>
  <files>hooks/useArchivingData.ts, components/subcommittees/ArchivingPanel.tsx</files>
  <action>
Przeczytaj oba pliki: hooks/useArchivingData.ts i components/subcommittees/ArchivingPanel.tsx.

--- ZMIANY W hooks/useArchivingData.ts ---

Sprawdź query archive_folders. Jeśli używa select('*'), folder_type zostanie zwrócone automatycznie po migracji. Jeśli select wymienia pola jawnie (np. select('id, title, status')), dodaj 'folder_type' do listy pól.

Upewnij się że typ zmiennej archiveFolders to `ArchiveFolder[]` (nie `any[]`) i że import type z ../../types zawiera ArchiveFolder.

--- ZMIANY W components/subcommittees/ArchivingPanel.tsx ---

KROK 1 — Dodaj import logAudit (po istniejących importach Supabase, przed import type):
```typescript
import { logAudit } from '../../lib/audit'
```

KROK 2 — Dodaj import FolderType do bloku import type:
```typescript
import type { ArchiveFolder, Petition, AppUser, CaseAttachment, Case, FolderType } from '../../types'
```

KROK 3 — Dodaj pole folder_type do stanu formularza tworzenia folderu:
W istniejącym useState archiveForm zmień definicję na:
```typescript
const [archiveForm, setArchiveForm] = useState({
  title: '',
  status: 'W przygotowaniu',
  notes: '',
  folder_type: 'general' as FolderType,
})
```

KROK 4 — Dodaj select w formularzu tworzenia folderu (w modalu tworzenia, po polu "Notatki"):
```tsx
<div>
  <label className="block text-sm font-bold text-slate-700 dark:text-slate-300 mb-1">Typ teczki</label>
  <select
    value={archiveForm.folder_type}
    onChange={e => setArchiveForm({ ...archiveForm, folder_type: e.target.value as FolderType })}
    className="w-full px-3 py-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-slate-900 dark:text-white text-sm"
  >
    <option value="general">Ogólna</option>
    <option value="project_report">Raport projektowy</option>
  </select>
</div>
```

KROK 5 — Utwórz helper renderFolderTypeBadge (jako stała funkcja przed return komponentu lub jako inline JSX):
```typescript
const getFolderTypeBadge = (folderType: FolderType) => {
  if (folderType === 'project_report') {
    return <span className="px-2 py-0.5 rounded-full text-xs font-bold bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-400">Raport projektowy</span>
  }
  return <span className="px-2 py-0.5 rounded-full text-xs font-bold bg-slate-100 dark:bg-slate-700 text-slate-600 dark:text-slate-300">Ogólna</span>
}
```

KROK 6 — Dodaj badge do listy folderów w JSX:
W miejscu gdzie renderowane są karty folderów (archiveFolders.map(...)), w każdej karcie dodaj `{getFolderTypeBadge(folder.folder_type ?? 'general')}` obok tytułu lub statusu folderu.

KROK 7 — Zaktualizuj updateArchiveStatus aby logował zmianę statusu:
Znajdź funkcję updateArchiveStatus. Pobierz aktualny status przed update i wywołaj logAudit:
```typescript
const updateArchiveStatus = async (id: string, newStatus: string) => {
  if (!currentUser) return
  // Pobierz stary status
  const folder = archiveFolders.find(f => f.id === id)
  const oldStatus = folder?.status ?? null
  await supabase.from('archive_folders').update({ status: newStatus }).eq('id', id)
  // Audit log
  await logAudit({
    userId: currentUser.id,
    action: 'STATUS_CHANGED',
    entityType: 'archive_folder',
    entityId: id,
    oldValue: oldStatus ? { status: oldStatus } : null,
    newValue: { status: newStatus },
  })
  await onRefetch()
}
```

WAŻNE: Nie usuwaj żadnej istniejącej funkcjonalności (drag upload, drawer, usunięcie folderu). Tylko dodawaj i modyfikuj updateArchiveStatus.

Sprawdź że po resecie formularza (po handleAddArchiveFolder) folder_type wraca do 'general':
```typescript
setArchiveForm({ title: '', status: 'W przygotowaniu', notes: '', folder_type: 'general' })
```
  </action>
  <verify>
    <automated>grep -c "logAudit" components/subcommittees/ArchivingPanel.tsx && grep -c "folder_type" components/subcommittees/ArchivingPanel.tsx && grep -c "getFolderTypeBadge\|folder_type.*badge\|Raport projektowy" components/subcommittees/ArchivingPanel.tsx</automated>
  </verify>
  <done>ArchivingPanel zawiera badge typu folderu (ogólna/raport projektowy) na kartach folderów, formularz tworzenia ma select z typem folderu, updateArchiveStatus wywołuje logAudit() z oldValue i newValue statusu.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — brak błędów TypeScript
- `grep "logAudit" components/subcommittees/ArchivingPanel.tsx` — import i wywołanie z 'archive_folder'
- `grep "folder_type" hooks/useArchivingData.ts` — pole pobierane z bazy lub select('*')
- `grep "folder_type" components/subcommittees/ArchivingPanel.tsx` — pole używane w formularzu i badge
- `grep "Raport projektowy\|project_report" components/subcommittees/ArchivingPanel.tsx` — badge niebieski
- `grep "entityType.*archive_folder" components/subcommittees/ArchivingPanel.tsx` — audit log z właściwym typem encji
</verification>

<success_criteria>
- Foldery na liście mają badge: szary "Ogólna" lub niebieski "Raport projektowy" (per D-05)
- Modal tworzenia folderu ma select z opcjami: Ogólna / Raport projektowy
- Zmiana statusu folderu rejestruje wpis w audit_log z action='STATUS_CHANGED', entityType='archive_folder'
- Brak błędów TypeScript
- Istniejąca funkcjonalność folderów i podań działa bez zmian
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/04-archiving-module/04-05-SUMMARY.md` z:
- Potwierdzeniem że ARCH-05 i ARCH-06 są spełnione
- Listą zmian w ArchivingPanel.tsx
- Notą o audit log (pattern logAudit wywołany z updateArchiveStatus)
- Potwierdzeniem że cała Faza 4 jest kompletna (ARCH-01 do ARCH-06)
</output>
