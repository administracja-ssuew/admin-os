---
phase: 04-archiving-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260406_archiving_module.sql
  - types/index.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-05

must_haves:
  truths:
    - "Tabela meeting_protocols istnieje w bazie z polami: id, title, date, participants, agenda, findings, actions, protocol_status, file_url, file_name"
    - "Kolumna folder_type istnieje w archive_folders z wartościami: general, project_report"
    - "RLS na meeting_protocols pozwala na odczyt uwierzytelnionym użytkownikom i zapis przez service role"
    - "Interfejs MeetingProtocol w types/index.ts odzwierciedla nową strukturę tabeli"
    - "Interfejs ArchiveFolder w types/index.ts ma pole folder_type"
  artifacts:
    - path: "supabase/migrations/20260406_archiving_module.sql"
      provides: "SQL migracji tworzącej meeting_protocols i dodającej folder_type"
      contains: "CREATE TABLE meeting_protocols"
    - path: "types/index.ts"
      provides: "Zaktualizowane interfejsy TypeScript"
      exports: ["MeetingProtocol", "ArchiveFolder", "FolderType"]
  key_links:
    - from: "types/index.ts"
      to: "supabase/migrations/20260406_archiving_module.sql"
      via: "Interfejsy TypeScript muszą odpowiadać schematowi DB"
      pattern: "FolderType|protocol_status|meeting_protocols"
---

<objective>
Stworzyć migrację SQL dla tabel modułu archiwizacji oraz zaktualizować typy TypeScript.

Purpose: Fundament danych dla całego modułu — bez tej migracji żaden inny plan fazy nie może działać. Zaktualizowane typy zapewniają poprawność typowania we wszystkich kolejnych planach.
Output: Plik migracji SQL gotowy do uruchomienia, zaktualizowany types/index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-archiving-module/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@types/index.ts

<interfaces>
<!-- Istniejące typy do modyfikacji -->

Z types/index.ts (aktualne, wymagają rozszerzenia):

```typescript
export type ArchiveFolderStatus = 'W przygotowaniu' | 'Aktywna' | 'Zamknięta'

export interface ArchiveFolder {
  id: string
  title: string
  status: ArchiveFolderStatus
  notes: string | null
  attachments: CaseAttachment[]
  created_at: string
  // BRAKUJE: folder_type
}

// Obecny MeetingProtocol (niewystarczający — zastąpić):
export interface MeetingProtocol {
  id: string
  meeting_id: string       // Stare podejście — nowa tabela jest niezależna
  content: string
  status: ProtocolStatus
  created_at: string
}
```

Z lib/audit.ts:
```typescript
export async function logAudit({ userId, action, entityType, entityId, oldValue, newValue }: AuditLogParams): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migracja SQL — meeting_protocols i folder_type</name>
  <files>supabase/migrations/20260406_archiving_module.sql</files>
  <action>
Utwórz plik migracji SQL. Plik musi zawierać DOKŁADNIE:

1. Enum protocol_status jeśli nie istnieje:
```sql
DO $$ BEGIN
  CREATE TYPE protocol_status AS ENUM ('draft', 'finalized');
EXCEPTION WHEN duplicate_object THEN NULL;
END $$;
```

2. Enum folder_type:
```sql
DO $$ BEGIN
  CREATE TYPE folder_type AS ENUM ('general', 'project_report');
EXCEPTION WHEN duplicate_object THEN NULL;
END $$;
```

3. Tabela meeting_protocols:
```sql
CREATE TABLE IF NOT EXISTS meeting_protocols (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  title         text NOT NULL,
  date          date NOT NULL,
  participants  text NOT NULL DEFAULT '',
  agenda        text NOT NULL DEFAULT '',
  findings      text NOT NULL DEFAULT '',
  actions       text NOT NULL DEFAULT '',
  protocol_status protocol_status NOT NULL DEFAULT 'draft',
  file_url      text,
  file_name     text,
  created_by    uuid REFERENCES auth.users(id),
  created_at    timestamptz NOT NULL DEFAULT now(),
  updated_at    timestamptz NOT NULL DEFAULT now()
);
```

4. RLS na meeting_protocols:
```sql
ALTER TABLE meeting_protocols ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Authenticated users can read protocols"
  ON meeting_protocols FOR SELECT
  TO authenticated
  USING (true);

CREATE POLICY "Service role can insert protocols"
  ON meeting_protocols FOR INSERT
  TO service_role
  WITH CHECK (true);

CREATE POLICY "Service role can update protocols"
  ON meeting_protocols FOR UPDATE
  TO service_role
  USING (true)
  WITH CHECK (true);
```

Uwaga: W tym projekcie mutacje wykonywane są przez supabase client (anon key), nie przez service role. Dlatego dodaj też:
```sql
CREATE POLICY "Authenticated users can insert protocols"
  ON meeting_protocols FOR INSERT
  TO authenticated
  WITH CHECK (true);

CREATE POLICY "Authenticated users can update protocols"
  ON meeting_protocols FOR UPDATE
  TO authenticated
  USING (true)
  WITH CHECK (true);
```

5. Kolumna folder_type w archive_folders:
```sql
ALTER TABLE archive_folders
  ADD COLUMN IF NOT EXISTS folder_type folder_type NOT NULL DEFAULT 'general';
```

Uruchom migrację przez Supabase CLI lub przekaż do ręcznego wykonania w SQL Editorze.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS meeting_protocols" supabase/migrations/20260406_archiving_module.sql && grep -c "folder_type" supabase/migrations/20260406_archiving_module.sql</automated>
  </verify>
  <done>Plik SQL istnieje, zawiera CREATE TABLE meeting_protocols, definicję enumu folder_type oraz ALTER TABLE archive_folders ADD COLUMN folder_type.</done>
</task>

<task type="auto">
  <name>Task 2: Aktualizacja types/index.ts</name>
  <files>types/index.ts</files>
  <action>
Przeczytaj plik types/index.ts. Wprowadź następujące zmiany:

1. Dodaj nowy typ FolderType zaraz przed interfejsem ArchiveFolder:
```typescript
export type FolderType = 'general' | 'project_report'
```

2. Rozszerz interfejs ArchiveFolder o pole folder_type:
```typescript
export interface ArchiveFolder {
  id: string
  title: string
  status: ArchiveFolderStatus
  folder_type: FolderType          // DODANE
  notes: string | null
  attachments: CaseAttachment[]
  created_at: string
}
```

3. Zastąp CAŁY obecny interfejs MeetingProtocol nową wersją (stary jest oparty na meeting_id — niepotrzebny):
```typescript
export interface MeetingProtocol {
  id: string
  title: string
  date: string                    // format: YYYY-MM-DD
  participants: string
  agenda: string
  findings: string
  actions: string
  protocol_status: ProtocolStatus
  file_url: string | null
  file_name: string | null
  created_by: string | null
  created_at: string
  updated_at: string
}
```

Typ ProtocolStatus ('draft' | 'finalized') jest już zdefiniowany w pliku — nie duplikuj.

WAŻNE: Zachowaj wszystkie inne typy w pliku bez zmian. Używaj single quotes, bez średników, 2-space indent (konwencje projektu).
  </action>
  <verify>
    <automated>grep -c "FolderType" types/index.ts && grep -c "folder_type: FolderType" types/index.ts && grep -c "participants: string" types/index.ts</automated>
  </verify>
  <done>types/index.ts zawiera FolderType, pole folder_type w ArchiveFolder, i nowy interfejs MeetingProtocol z polami: title, date, participants, agenda, findings, actions, file_url, file_name.</done>
</task>

</tasks>

<verification>
Po obu zadaniach sprawdź:
- `grep "CREATE TABLE IF NOT EXISTS meeting_protocols" supabase/migrations/20260406_archiving_module.sql` — zwraca wynik
- `grep "folder_type folder_type" supabase/migrations/20260406_archiving_module.sql` — zwraca wynik
- `grep "FolderType" types/index.ts` — zwraca przynajmniej 2 wyniki (definicja + użycie)
- `grep "participants: string" types/index.ts` — zwraca wynik (nowy MeetingProtocol)
- `npx tsc --noEmit` — brak błędów TS (lub nowe błędy tylko w plikach, które będą zaktualizowane w kolejnych planach)
</verification>

<success_criteria>
- Plik migracji SQL istnieje i jest syntaktycznie poprawny
- types/index.ts kompiluje się bez błędów (tsc --noEmit)
- Interfejs MeetingProtocol ma pola: title, date, participants, agenda, findings, actions, file_url, file_name
- Interfejs ArchiveFolder ma pole folder_type: FolderType
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/04-archiving-module/04-01-SUMMARY.md` z:
- Polami tabeli meeting_protocols (lista)
- Zmianami w types/index.ts
- Statusem migracji (czy uruchomiona w bazie)
</output>
