---
phase: 04-archiving-module
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - components/subcommittees/ArchivingPanel.tsx
autonomous: true
requirements:
  - ARCH-04

must_haves:
  truths:
    - "ArchivingPanel posiada dwie zakładki: 'Foldery i Podania' (istniejące) oraz 'Sprawy' (nowa)"
    - "Zakładka Sprawy wyświetla Kanban z trzema kolumnami: 'Nowe', 'W toku', 'Zamknięte'"
    - "Kanban pokazuje wyłącznie sprawy (cases) przypisane do departamentu bieżącego użytkownika"
    - "Karty sprawy zawierają: numer sprawy, tytuł, typ sprawy, właściciel (imię nazwisko)"
    - "Filtrowanie po department_id działa — inne departamenty nie widzą tych spraw"
  artifacts:
    - path: "components/subcommittees/ArchivingPanel.tsx"
      provides: "Zakładka Sprawy z Kanban"
      contains: "useCases|activeTab|Sprawy"
      min_lines: 50
  key_links:
    - from: "ArchivingPanel — zakładka Sprawy"
      to: "useCases() hook"
      via: "import useCases, filtrowanie cases.filter(c => c.department_id === currentUser?.department_id)"
      pattern: "useCases|department_id.*currentUser"
---

<objective>
Dodać zakładkę "Sprawy" z widokiem Kanban do ArchivingPanel, używając istniejącego hooka useCases z filtrem po department_id.

Purpose: ARCH-04 — Kanban spraw w module Archiwizacji, wyłącznie sprawy bieżącej podkomisji. Per D-04: osobna zakładka "Sprawy" w ArchivingPanel (obok zakładki folderów).
Output: ArchivingPanel z dwoma zakładkami, zakładka Sprawy pokazuje Kanban przefiltrowany po dept.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-archiving-module/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@components/subcommittees/ArchivingPanel.tsx
@types/index.ts

<interfaces>
<!-- Interfejsy do użycia -->

```typescript
// useCases hook (hooks/useCases.ts):
import { useCases } from '../../hooks/useCases'
// Zwraca: { cases: Case[], loading: boolean, refetch: () => Promise<void> }

// Case type (types/index.ts):
export type CaseStatus = 'new' | 'in_progress' | 'closed'
export interface Case {
  id: string
  title: string
  case_number: string
  case_type: string
  status: CaseStatus
  owner_id: string | null
  department_id: string | null
  // relacje:
  users?: { first_name: string; last_name: string } | null
  departments?: { name: string } | null
}

// ArchivingPanelProps (istniejące):
export interface ArchivingPanelProps {
  archiveFolders: ArchiveFolder[]
  petitions: Petition[]
  members: DeptMember[]
  currentUser: AppUser | null
  isAdmin: boolean
  onRefetch: () => Promise<void>
}
```

Wzorzec Kanban z app/tasks/page.tsx:
```typescript
const columns = [
  { id: 'new', title: 'Nowe', color: 'bg-slate-100 dark:bg-slate-800/40 border-slate-200 dark:border-slate-700/50', textColor: 'text-slate-600 dark:text-slate-400' },
  { id: 'in_progress', title: 'W Toku', color: 'bg-blue-50 dark:bg-blue-900/20 border-blue-200 dark:border-blue-900/50', textColor: 'text-blue-700 dark:text-blue-400' },
  { id: 'closed', title: 'Zamknięte', color: 'bg-green-50 dark:bg-green-900/10 border-green-200 dark:border-green-900/50', textColor: 'text-green-700 dark:text-green-500' },
]
// Kolumna: grid col, karty filtrowane po status
// Karta: bg-white dark:bg-slate-800 p-4 rounded-xl border border-slate-200 dark:border-slate-700
```

CaseStatus mapowanie na polskie etykiety:
- 'new' → 'Nowe'
- 'in_progress' → 'W toku'
- 'closed' → 'Zamknięte'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zakładki i Kanban spraw w ArchivingPanel</name>
  <files>components/subcommittees/ArchivingPanel.tsx</files>
  <action>
Przeczytaj cały plik components/subcommittees/ArchivingPanel.tsx. Dodaj zakładkę "Sprawy" z Kanban.

KROK 1 — Dodaj import useCases (po istniejących importach, przed import type):
```typescript
import { useCases } from '../../hooks/useCases'
```

Dodaj do import type (rozszerz istniejący):
```typescript
import type { ArchiveFolder, Petition, AppUser, CaseAttachment, Case } from '../../types'
```

KROK 2 — Dodaj stan zakładek na początku komponentu (po istniejących useState):
```typescript
const [activeTab, setActiveTab] = useState<'folders' | 'cases'>('folders')
```

KROK 3 — Użyj hook useCases wewnątrz komponentu:
```typescript
const { cases, loading: casesLoading } = useCases()
```

KROK 4 — Oblicz przefiltrowane sprawy (derived value, przed return):
```typescript
const deptCases = cases.filter(c => c.department_id === currentUser?.department_id)
```

KROK 5 — Zdefiniuj kolumny Kanban (stała lokalna, przed return):
```typescript
const caseColumns: { id: CaseStatus; title: string; color: string; textColor: string }[] = [
  { id: 'new', title: 'Nowe', color: 'bg-slate-100 dark:bg-slate-800/40 border border-slate-200 dark:border-slate-700/50', textColor: 'text-slate-600 dark:text-slate-400' },
  { id: 'in_progress', title: 'W Toku', color: 'bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-900/50', textColor: 'text-blue-700 dark:text-blue-400' },
  { id: 'closed', title: 'Zamknięte', color: 'bg-green-50 dark:bg-green-900/10 border border-green-200 dark:border-green-900/50', textColor: 'text-green-700 dark:text-green-500' },
]
```

KROK 6 — Zaktualizuj JSX return:

Zawiń CAŁE obecne JSX (div z flex flex-col gap-8 itd.) w nową strukturę z zakładkami:

```tsx
return (
  <div className="flex flex-col gap-6">
    {/* Zakładki */}
    <div className="flex gap-2 border-b border-slate-200 dark:border-slate-700">
      <button
        onClick={() => setActiveTab('folders')}
        className={`px-4 py-2 text-sm font-bold transition-colors border-b-2 -mb-px ${
          activeTab === 'folders'
            ? 'border-blue-600 text-blue-600 dark:text-blue-400'
            : 'border-transparent text-slate-500 dark:text-slate-400 hover:text-slate-700 dark:hover:text-slate-300'
        }`}
      >
        Foldery i Podania
      </button>
      <button
        onClick={() => setActiveTab('cases')}
        className={`px-4 py-2 text-sm font-bold transition-colors border-b-2 -mb-px ${
          activeTab === 'cases'
            ? 'border-blue-600 text-blue-600 dark:text-blue-400'
            : 'border-transparent text-slate-500 dark:text-slate-400 hover:text-slate-700 dark:hover:text-slate-300'
        }`}
      >
        Sprawy
      </button>
    </div>

    {/* Zawartość zakładki Foldery i Podania */}
    {activeTab === 'folders' && (
      <div className="flex flex-col gap-8">
        {/* TUTAJ wklej CAŁĄ dotychczasową zawartość return (div z grid cols itd.) */}
      </div>
    )}

    {/* Zawartość zakładki Sprawy — Kanban */}
    {activeTab === 'cases' && (
      <div>
        <h2 className="text-lg font-bold text-slate-900 dark:text-white mb-4">Sprawy działu</h2>
        {casesLoading ? (
          <div className="flex items-center justify-center h-32">
            <Loader2 className="animate-spin text-slate-400" size={24} />
          </div>
        ) : deptCases.length === 0 ? (
          <div className="text-center py-12 text-slate-500 dark:text-slate-400">
            <p className="font-bold text-lg mb-1">Brak spraw</p>
            <p className="text-sm">Dział nie ma przypisanych spraw w systemie.</p>
          </div>
        ) : (
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            {caseColumns.map(col => {
              const colCases = deptCases.filter(c => c.status === col.id)
              return (
                <div key={col.id} className={`rounded-xl p-3 ${col.color}`}>
                  <div className={`text-xs font-bold uppercase tracking-wider mb-3 flex items-center justify-between ${col.textColor}`}>
                    <span>{col.title}</span>
                    <span className="bg-white dark:bg-slate-800 px-2 py-0.5 rounded-full text-slate-600 dark:text-slate-400">{colCases.length}</span>
                  </div>
                  <div className="flex flex-col gap-2">
                    {colCases.map(c => (
                      <div
                        key={c.id}
                        className="bg-white dark:bg-slate-800 p-3 rounded-xl border border-slate-200 dark:border-slate-700 shadow-sm"
                      >
                        <div className="flex items-center justify-between mb-1">
                          <span className="text-[10px] font-bold text-slate-500 dark:text-slate-400 tracking-wider">{c.case_number}</span>
                          <span className="text-[10px] px-1.5 py-0.5 rounded bg-slate-100 dark:bg-slate-700 text-slate-500 dark:text-slate-400 font-bold">{c.case_type}</span>
                        </div>
                        <p className="text-sm font-bold text-slate-900 dark:text-white leading-tight">{c.title}</p>
                        {c.users && (
                          <p className="text-xs text-slate-500 dark:text-slate-400 mt-1">
                            {c.users.first_name} {c.users.last_name}
                          </p>
                        )}
                      </div>
                    ))}
                    {colCases.length === 0 && (
                      <p className="text-xs text-slate-400 dark:text-slate-600 text-center py-4">Brak spraw</p>
                    )}
                  </div>
                </div>
              )
            })}
          </div>
        )}
      </div>
    )}
  </div>
)
```

WAŻNE: Obecna zawartość return (div z grid cols) musi trafić WEWNĄTRZ bloku `activeTab === 'folders'`. Nie usuwaj żadnego kodu folderów/podań.

Zaimportuj `CaseStatus` z types jeśli potrzebny dla typowania caseColumns. Upewnij się że Loader2 jest już w importach z lucide-react (jest w obecnym pliku).
  </action>
  <verify>
    <automated>grep -c "useCases" components/subcommittees/ArchivingPanel.tsx && grep -c "activeTab" components/subcommittees/ArchivingPanel.tsx && grep -c "deptCases" components/subcommittees/ArchivingPanel.tsx</automated>
  </verify>
  <done>ArchivingPanel renderuje dwie zakładki. Zakładka "Sprawy" wyświetla Kanban z kolumnami Nowe/W Toku/Zamknięte z kartami spraw przefiltrowanych po department_id użytkownika. Brak błędów TypeScript.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — brak błędów w ArchivingPanel.tsx
- `grep "useCases" components/subcommittees/ArchivingPanel.tsx` — import i wywołanie hooka
- `grep "department_id.*currentUser\|currentUser.*department_id" components/subcommittees/ArchivingPanel.tsx` — filtrowanie po dept
- `grep "activeTab.*cases\|cases.*activeTab" components/subcommittees/ArchivingPanel.tsx` — zakładka Cases
- `grep "caseColumns\|colCases" components/subcommittees/ArchivingPanel.tsx` — kolumny Kanban
</verification>

<success_criteria>
- ArchivingPanel ma dwie zakładki: "Foldery i Podania" i "Sprawy"
- Kanban w zakładce Sprawy filtruje cases po currentUser.department_id
- Trzy kolumny: new / in_progress / closed
- Istniejąca funkcjonalność folderów i podań niezmieniona
- Brak błędów TypeScript
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/04-archiving-module/04-04-SUMMARY.md` z:
- Potwierdzeniem że oba taxy (folders + cases) działają
- Notą o filtrze department_id (jak jest zastosowany)
- Liczbą linii pliku po zmianach
</output>
