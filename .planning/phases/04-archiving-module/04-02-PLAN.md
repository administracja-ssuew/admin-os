---
phase: 04-archiving-module
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/meetings/page.tsx
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "Strona /meetings pokazuje listę istniejących protokołów z tabeli meeting_protocols"
    - "Użytkownik może kliknąć 'Nowy protokół' i wypełnić formularz z polami: tytuł, data, uczestnicy, porządek obrad, ustalenia, akcje"
    - "Zapisany protokół pojawia się na liście z badge statusu (Szkic / Zablokowany)"
    - "Kliknięcie protokołu otwiera panel boczny (drawer) z podglądem i edycją pól"
    - "Protokół w statusie 'finalized' ma pola zablokowane (disabled) — nie można edytować"
  artifacts:
    - path: "app/meetings/page.tsx"
      provides: "Strona edytora protokołów"
      contains: "meeting_protocols"
      min_lines: 150
  key_links:
    - from: "app/meetings/page.tsx"
      to: "supabase table: meeting_protocols"
      via: "supabase.from('meeting_protocols').select/insert/update"
      pattern: "from\\('meeting_protocols'\\)"
    - from: "formularz edycji"
      to: "protocol_status === 'finalized'"
      via: "disabled prop na input/textarea"
      pattern: "disabled.*finalized|finalized.*disabled"
---

<objective>
Przebudować stronę /meetings: zastąpić istniejącą stronę spotkań nowym edytorem protokołów posiedzeń opartym na tabeli meeting_protocols.

Purpose: ARCH-01 — Strona /meetings z edytorem protokołu wypełnianym z szablonu. Obecna strona operuje na tabeli meetings (spotkania), nowa będzie operować na meeting_protocols (protokoły posiedzeń). Per D-01: pełna strona, nie slide-over.
Output: Kompletna strona /meetings z listą protokołów i formularzem edycji.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-archiving-module/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/04-archiving-module/04-01-SUMMARY.md
@types/index.ts

<interfaces>
<!-- Interfejsy z 04-01 (po migracji) -->

```typescript
// Z types/index.ts po 04-01:
export type ProtocolStatus = 'draft' | 'finalized'
export type FolderType = 'general' | 'project_report'

export interface MeetingProtocol {
  id: string
  title: string
  date: string                    // format: YYYY-MM-DD
  participants: string
  agenda: string
  findings: string
  actions: string
  protocol_status: ProtocolStatus
  file_url: string | null
  file_name: string | null
  created_by: string | null
  created_at: string
  updated_at: string
}
```

Konwencje projektu (z CONVENTIONS.md):
- 'use client' jako pierwsza linia
- Single quotes, brak średników, 2-space indent
- Import types z `import type { ... }` zawsze ostatnie
- Względne ścieżki importów (nie @/* alias)
- Mutacje: toast.loading → toast.success/error z tym samym id
- Layout: `<div className="flex min-h-screen bg-slate-50 dark:bg-slate-900"><Sidebar /><div className="flex-1 ml-64 p-8">`
- Karta: `bg-white dark:bg-slate-800 rounded-2xl border border-slate-200 dark:border-slate-700 p-5`
- Przycisk primary: `px-4 py-2 bg-blue-600 text-white font-bold rounded-lg hover:bg-blue-700 transition-colors`
- Badge: `px-2 py-1 rounded-full text-xs font-bold`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lista protokołów i formularz tworzenia</name>
  <files>app/meetings/page.tsx</files>
  <action>
Zastąp CAŁĄ zawartość app/meetings/page.tsx nową implementacją. Obecna strona obsługuje tabelę meetings — przebudowujemy ją pod meeting_protocols.

STRUKTURA KOMPONENTU (kolejność wg CONVENTIONS.md):
1. 'use client'
2. Importy: useState, useEffect, useCallback z react; supabase z ../../lib/supabase; Sidebar z ../../components/Sidebar; ikony z lucide-react; toast z react-hot-toast; useCurrentUser z ../../hooks/useCurrentUser; import type { MeetingProtocol, ProtocolStatus } z ../../types
3. Stała szablonu:
```typescript
const EMPTY_FORM = {
  title: '',
  date: new Date().toISOString().split('T')[0],
  participants: '',
  agenda: '',
  findings: '',
  actions: '',
}
```
4. Komponent MeetingsPage z:
   - `const { user, loading: userLoading } = useCurrentUser()`
   - `const [protocols, setProtocols] = useState<MeetingProtocol[]>([])`
   - `const [loading, setLoading] = useState(true)`
   - `const [isModalOpen, setIsModalOpen] = useState(false)`
   - `const [isSubmitting, setIsSubmitting] = useState(false)`
   - `const [form, setForm] = useState(EMPTY_FORM)`
   - `const [selectedProtocol, setSelectedProtocol] = useState<MeetingProtocol | null>(null)`
   - `const [isDrawerOpen, setIsDrawerOpen] = useState(false)`
   - `const [editForm, setEditForm] = useState(EMPTY_FORM)`

5. fetchProtocols (useCallback):
```typescript
const fetchProtocols = useCallback(async () => {
  const { data } = await supabase
    .from('meeting_protocols')
    .select('*')
    .order('date', { ascending: false })
  if (data) setProtocols(data as MeetingProtocol[])
  setLoading(false)
}, [])
```

6. useEffect wywołujący fetchProtocols

7. handleCreate (submit nowego protokołu):
```typescript
const handleCreate = async (e: React.FormEvent) => {
  e.preventDefault()
  if (!user) return
  setIsSubmitting(true)
  const toastId = toast.loading('Tworzenie protokołu...')
  const { error } = await supabase.from('meeting_protocols').insert([{
    ...form,
    protocol_status: 'draft',
    created_by: user.id,
  }])
  if (!error) {
    toast.success('Protokół utworzony!', { id: toastId })
    setIsModalOpen(false)
    setForm(EMPTY_FORM)
    await fetchProtocols()
  } else {
    toast.error('Błąd podczas tworzenia protokołu', { id: toastId })
  }
  setIsSubmitting(false)
}
```

8. handleSaveEdit (zapis edytowanego protokołu z drawera):
```typescript
const handleSaveEdit = async () => {
  if (!selectedProtocol || selectedProtocol.protocol_status === 'finalized') return
  const toastId = toast.loading('Zapisywanie...')
  const { error } = await supabase.from('meeting_protocols')
    .update({ ...editForm, updated_at: new Date().toISOString() })
    .eq('id', selectedProtocol.id)
  if (!error) {
    toast.success('Zapisano!', { id: toastId })
    await fetchProtocols()
    setSelectedProtocol({ ...selectedProtocol, ...editForm })
  } else {
    toast.error('Błąd zapisu', { id: toastId })
  }
}
```

JSX LAYOUT:
- Pełna strona z Sidebar + nagłówek "Protokoły posiedzeń" + przycisk "Nowy protokół"
- Lista protokołów: karty z tytułem, datą, badge statusu (draft = szary "Szkic", finalized = niebieski "Zablokowany" z ikoną Lock)
- Badge draft: `px-2 py-1 rounded-full text-xs font-bold bg-slate-100 dark:bg-slate-700 text-slate-600 dark:text-slate-400`
- Badge finalized: `px-2 py-1 rounded-full text-xs font-bold bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-400`
- Kliknięcie karty: otwiera drawer (setSelectedProtocol + setEditForm z wartościami protokołu + setIsDrawerOpen(true))

MODAL tworzenia: sześć pól textarea/input (title=input, date=input type date, participants/agenda/findings/actions=textarea rows=3). Pole `isFinalized = selectedProtocol?.protocol_status === 'finalized'`.

DRAWER (panel boczny — fixed right-0, top-0, h-full, w-[480px], bg-white dark:bg-slate-900, shadow-2xl, z-50):
- Te same sześć pól co modal, ale disabled gdy `selectedProtocol.protocol_status === 'finalized'`
- Przycisk "Zapisz zmiany" disabled gdy finalized (planowany 04-03 doda przycisk Zablokuj)
- Jeśli finalized: komunikat/badge "Ten protokół jest zablokowany i nie można go edytować"

Upewnij się że działają loading state (SkeletonLoader jeśli istnieje, inaczej `<Loader2 className="animate-spin">`) i empty state gdy lista pusta.
  </action>
  <verify>
    <automated>grep -c "from('meeting_protocols')" app/meetings/page.tsx && grep -c "protocol_status" app/meetings/page.tsx && grep -c "isDrawerOpen" app/meetings/page.tsx</automated>
  </verify>
  <done>Strona /meetings renderuje się bez błędów TS, pobiera protokoły z meeting_protocols, umożliwia tworzenie nowych protokołów przez modal, kliknięcie protokołu otwiera drawer z edycją pól. Protokoły finalized mają pola disabled.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — brak błędów w app/meetings/page.tsx
- `grep "from('meeting_protocols')" app/meetings/page.tsx` — zwraca wyniki
- `grep "disabled" app/meetings/page.tsx` — pole disabled powiązane z finalized
- `grep "protocol_status.*draft" app/meetings/page.tsx` — insert ustawia status na draft
- Wizualnie: otwórz /meetings w przeglądarce — lista + przycisk "Nowy protokół" widoczne
</verification>

<success_criteria>
- Strona /meetings jest funkcjonalna: lista protokołów + tworzenie + edycja
- Protokół tworzony z protocol_status = 'draft'
- Pola edycji są disabled gdy protocol_status === 'finalized'
- Brak błędów TypeScript (tsc --noEmit)
</success_criteria>

<output>
Po ukończeniu utwórz `.planning/phases/04-archiving-module/04-02-SUMMARY.md` z:
- Listą stanów komponentu (useState)
- Listą zapytań Supabase (from/select/insert/update)
- Notą o tym które stany/handlery będą rozszerzone w 04-03
</output>
